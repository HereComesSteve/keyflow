# 错误记录

> 本项目踩过的坑，每次解决一个问题就记一条，方便以后查。

***

## 2026-08-30 蓝牙模块接收不到 Arduino 发送的数据（左右手校时超时）

### 现象

* 左右手蓝牙校时一直超时：左手反复打印 `Timing sync sent (0x30)` → `Timing timeout, retry`

* 左手发 0x30 右手收不到；右手发信标左手也收不到

* 但**手机**直连任意一个模块，收发都正常（能上传乐谱、马达正常震动）

* 查固件、引脚（pin 11 / pin 4 交替发送测试）、模块配置，全部正常

### 原因（根因）

**逻辑电平转换的分压电阻接反了。**

电路里 UNO 的 TX（5V）要经过 1kΩ / 2kΩ 两个电阻分压降到模块可接受的 3.3V 电平，才能接蓝牙模块的 RX。

分压公式：`Vout = Vcc × R2 / (R1 + R2)`，5V 经 1k + 2k 分压：

* 正确接法（1k 在上、2k 在下）：`Vout = 5 × 2 / (1 + 2) ≈ 3.3V`

* **接反了（2k 在上、1k 在下）：`Vout = 5 × 1 / (1 + 2) ≈ 1.7V`**

模块 RX 需要约 3.3V 高电平才能识别，1.7V 认不出来，导致「Arduino → 蓝牙模块」方向的字节全部丢失。而「手机 → 蓝牙模块 → Arduino」方向不受影响（模块 TX 直连，不经过分压），所以手机收发看起来一切正常。

### 教训

* **分压电阻的输出电压不是"想当然的 3.3V"，必须用欧姆定律算一遍：`Vout = Vcc × R2/(R1+R2)`**

* 当时被"模块标称 3.3V 供电"先入为主，直接以为输出就是 3.3V，忽略了是 5V 经过分压得到的

* 排查 UART 通信问题时，**先量模块 RX 引脚的空闲电平**（空闲应为高电平，Uno 发 5V 时应量到约 3.3V），这一步能秒杀分压/接线错误

### 排查方法备忘（本次用的有效套路）

1. **手机直连模块测接收**：手机发 → 模块 → Arduino，能收到说明模块和 RX 路径好
2. **手机直连模块测发送**：Arduino 经模块发 → 手机收，能收到说明 TX 路径好（用 nRF Connect 时要**手动开启 Notifications**，否则数据不显示）
3. **回环测试的坑**：SoftwareSerial 发送期间会关闭中断，导线直连回环的数据会被吞掉，**软串口做不了 pin2↔pin11 直连回环测试**，会误判引脚损坏
4. **Uno 板上标 TX 的灯**对应的是硬件串口（USB 那条路），**跟软串口引脚无关**，看它判断不了软串口好坏

### 解决过程回顾（完整链路）

1. **现象**：左手发 0x30 右手收不到，右手发信标左手也收不到，校时持续超时
2. **排除固件**：检查 git 历史，固件未回档、校时逻辑无改动；左右手单独测单手模式均正常
3. **初步诊断**：怀疑「Arduino 的 TX → 蓝牙模块的 RX → 模块发射」这条发送链路有问题
4. **定位方向的关键判断**：两台设备出现**完全相同**的故障 → 意外硬件损坏的概率极低 → 更可能是**操作/接线这类系统性问题** → 转向排查操作问题
5. **逐步收窄**：

   * 手机直连模块测接收 → 正常（接收链路好）

   * 手机直连模块测发送（nRF Connect）→ 手动开启 Notifications 后收到 `P11-OUT` → 发送链路也正常

   * 既然单片机、模块、软件全正常，问题只剩**接线/电平**这一个环节
6. **最终定位**：检查分压电阻 → 1k/2k 放反 → 模块 RX 高电平只有 1.7V → 方向确认

### 总结思考（通用方法论）

**当大量元件同时出现相同故障时，优先检查"操作"而不是"硬件"。**

* 单个元件坏了是偶然（硬件损坏），**多个元件都出同样的问题，偶然性极低**，几乎不可能是硬件同时损坏

* 此时故障大概率出在**共享的、系统性的环节**：接线方式、电平配置、供电、固件逻辑、模块配置等——这些是"所有人都会犯的操作问题"

* 排查顺序建议：先软件/配置 → 再接线/电平 → 最后才怀疑硬件损坏

* 本次正是先按"硬件坏了"的方向查了很久（引脚、模块、波特率），绕了远路；早一点应用"多个设备同故障 = 检查操作"这个原则，能直接跳到最后一步——量电平、看接线

### 协作经验：AI 写文件时不要打开该文件

* 2026-08-30 记录本文时，AI 把内容写入磁盘后，文件会**莫名回档**——后来发现是因为**用 Trae Work 自带的查看窗口开着这个文件**，查看器的旧缓冲区在某个时刻把磁盘上的新内容覆盖回去了

* 经验：**让 AI 写文件期间，不要在查看窗口/编辑器里打开同一个文件**，等写完成后再打开查看

* 如果发现文件内容"明明写进去了又消失"，优先怀疑有编辑器在覆盖，而不是 AI 没写

### 2026-08-30 BLE 丢包导致 EEPROM 缺指令（表现为"最后两条同时熄灭"）

* **现象**：写入 10 条 LED 亮灭指令后播放，效果变成 `1→12→123→1234→12345→1234→123→12→0`，最后"12"直接全灭，少了中间的"1"

* **根因**：**BLE 丢包**——EEPROM 里实际只写入了 9 条指令（缺最后一条"灭LED1"），播放到倒数第二条后读到结束标志 `FF 00`，触发循环重启把所有灯熄灭，看起来像"最后两条同时熄灭"

* **排查方法**（关键）：

  * 看固件串口 `EEPROM read: addr=...` 的地址是否**连续**（本次 0x12 处直接是 `tick=255` 结束标志，而非预期的第 10 条）

  * 写入完成后看 `Bytes written` 是否 = `条数×2 + 2`（10 条应为 22）

* **教训**：指令本身、tick 计算、固件逻辑都正确时，若效果少一步且像"两条同时"，优先怀疑**传输层丢包**，重写一次即可；不要误以为是 tick 写错去改指令

***

## 2026-08-31 EEPROM 工作异常（VCC 接触不良）

### 现象

* EEPROM 读写不正常（功能失灵）

* 排查时发现：**EEPROM 模块上的指示灯亮度偏低**（发暗，不是完全熄灭，也不是正常亮度）

### 原因（根因）

**指示灯亮度偏低 = VCC 引脚接触不良。**

* 只有 SDA、SCL 和芯片供电这几路是接好的、能正常工作的，VCC 这一脚没接牢时，灯才会呈现"亮但偏暗"的状态，而不是完全不亮

* 亮度偏低说明电流只能靠其他路微弱导通，VCC 的主供电在接触点上有很大损耗/时断时续

### 教训（排查套路）

* **检查 EEPROM 问题的固定顺序**：

  1. **先查 SDA、SCL 有没有接反**（I2C 数据线接反是 EEPROM 不工作最常见的原因，换回来即可）

  2. **再根据指示灯亮度判断 VCC 是否接触不良**：灯亮度正常 → 供电没问题；灯偏暗 → 重点检查 VCC 引脚是否插紧/虚焊/夹线是否夹到位；灯完全不亮 → 彻底没供电，查电源回路

* 指示灯是排查供电问题最直接的信号源，**不要跳过看灯这一步直接去怀疑芯片或程序**

***

## 2026-08-31 双手模式传乐谱必丢指令（BLE 转发链路丢包）

*注：与上一条「EEPROM 指示灯 VCC 接触不良」是**两回事**（VCC 问题是昨天发现并已解决的），这条是独立的新问题。*

### 现象

* 向手套写入 10 条指令（`00 81 40 83 00 87 40 8F ... 40 01`），正常应存 **11 条数据**（10 条指令 + `FF 00` 结束标志），`Bytes written` 应为 **22**（11×2）

* 实际只有 **10 条**，`Bytes written: 20`（10×2）——**丢的正是最后/中间的某条指令**

* LED 亮灯效果缺阶段：如写入 `1→12→123→1234→12345→1234→123→12→1`，实际看到 `1→1234→12345→1234→123→12→1`，**`123`** **阶段消失**（对应丢的指令是 `00 87`）

* 若丢的是 `FF 00` 结束标志：EEPROM 读到擦除态 `0xFF`，固件把它当结束标志 → `loop restart` 全灭重启

### 规律（关键）

| 场景              | 条数          | Bytes written | 结果    |
| --------------- | ----------- | ------------- | ----- |
| 单手模式 → 左手（主机直连） | 11（含 FF 00） | 22            | 正常    |
| 单手模式 → 右手（主机转发） | 10          | 20            | 丢 1 条 |
| 双手模式 → 左右手      | 左右都 10      | 20            | 丢 1 条 |

* **凡经过「主机 → 蓝牙转发 → 从机」链路的写入必丢末尾字节**，且稳定复现——不是随机干扰（随机不会"必丢"）

* 左手直连走 Web Bluetooth → 本地串口，链路可靠不丢；问题出在**转发链路**

* 疑似方向：最后一条指令发出后链路立即收尾（或从机 I2C 写入完成就关闭接收），尾部字节没排空即被截断——**发送时序/缓冲区问题**

### 排查方法（判据）

* 看 `Bytes written` 是否 = `条数×2 + 2`：10 条指令应为 22，只有 20 说明丢了 1 条

* 把主机转发日志 `[M->S] 0xFB转发` 列表与要写入的指令序列**逐条比对**，找出缺失的那条（本次缺第 3 条 `00 87`，日志里直接是 `40 8F`）

* 播放时首地址 `addr=0x0` 就读到 `tick=255` → 结束标志在开头/数据全空，写入没落盘

### 教训（与 VCC 问题区分）

* **`I2C write OK`** **只代表 I2C 事务完成，不代表指令全部到达**——写少条要看传输层（BLE），写不进则看供电/接线（VCC），两者现象判别：`Bytes written` 少 ✓ 丢包；`I2C ACK` 后读回 `0xFF` ✗ 才像供电问题

* 双手/右手目标丢包**必发**时，先查转发链路发送时序，不要怀疑指令本身、tick 计算或固件逻辑

***

## 2026-09-01 电脑蓝牙没开导致连不上（最基础的坑）

### 现象

* 点连接一直没有设备列表、连不上手套

### 原因

**Windows 系统蓝牙适配器没开。** 设备都没开机，上层怎么重试扫描都是白搭。

### 教训

* 排除顺序最前面先确认：**系统蓝牙开关**、蓝牙在设备管理器里没被禁用（曾误以为设备列表的"未知或不支持的设备"是过滤 bug，结果排查半天发现只是蓝牙没开）

***

## 2026-09-01 设备列表充满「未知或不支持的设备」

### 现象

* 打开蓝牙后扫描，设备列表里混入大量「未知或不支持的设备」占位项，真正的 Glove\_L 反而不好找

### 原因

* 豆包版主进程 `select-bluetooth-device` 的设备选择过滤逻辑**只看名称前缀（Glove），没过滤掉无名称/系统占位的设备**，导致透传了一堆垃圾进列表

### 解决

* 修改 `src/main/index.ts`：排除**无名称**或名称含「未知/不支持」的设备，保留所有正常命名的设备，列表保持干净

### 教训

* 设备过滤要分两层：**扫描源头（requestDevice 过滤器）和**主进程转发（select-bluetooth-device）。任一层漏了，列表就会脏

***

## 2026-09-01 Arduino IDE 串口监视器占用 COM 口，程序打不开串口

### 现象

* 运行自动化测试时 `Error: cannot open port \\.\COM6: 系统找不到指定的文件`，烧录时报 `unable to open port COM6`

### 原因

* **一个串口同时只能被一个程序占用**：Arduino IDE 的串口监视器一直开着 COM6，测试程序/烧录工具就打不开

### 解决

* 跑测试/烧录前**先关掉 Arduino IDE 的串口监视器**

### 教训

* 串口相关工具链的固定前置检查：**关串口监视器 → 再烧录/测试**。这一步不进脑子，后面所有串口操作都会莫名失败

***

## 2026-09-01 蓝牙从 2/11 挪到 0/1 后板载 USB 串口监视器失效

### 现象

* 为了 8MHz 自制板把蓝牙挪到硬件 UART（0/1）后，UNO 板载 USB 串口监视器收不到任何调试日志了

### 原因（根因）

* **板载 USB-TTL 芯片物理上就接在 D0/D1 上**，蓝牙占用 0/1 后和它抢同一对引脚，USB 虚拟串口自然废了

### 解决

* 蓝牙移回 `SoftwareSerial(2, 11)`；硬件串口 `Serial(0/1)` 专留给调试监视器（115200）。8MHz 自制板场景以后再用硬件 UART

### 教训

* UNO 的板载 USB 串口 ≠ 独立串口，它**物理占用 D0/D1**；挪串口前先想清楚调试输出放哪

***

## 2026-09-01 App 显示「从机链路正常」但右手根本没上电（链路状态是摆设）

### 现象

* 只给左手上电，App 面板却显示从机链路正常

### 原因（根因）

* 固件的心跳检测没真正生效：`heartbeatMiss` 从未递增、`awaitingPong` 状态没管理，从机断电后断链判定永远不触发；且 App 连接时从机若已断电，之前测到的状态残留，不会重新评估

### 解决（分三步修复，见下两条）

1. `handleHeartbeat`：**每轮都重发 PING**，若上一轮 PONG 未回则先计一次 miss，再判断断链（修复 miss 不涨/死锁）
2. `checkSlaveLink`：`heartbeatMiss >= HEARTBEAT_MAX_MISS` 时判定断链并上报 `EVT_SLAVE_LINK_LOST`
3. `CMD_HELLO` 握手时重置链路状态（`lastHeartbeatTime=0; awaitingPong=false; heartbeatMiss=0; slaveLinkAlive=true`），App 重连后**重新评估从机在线性**

### 教训

* 「链路状态」必须靠**真实心跳往返**驱动，不能是上电初始化的乐观值；每次有人眼皮底下测出"假正常"，先查 miss 计数器和 await 状态有没有真的在跑

***

## 2026-09-01 批量写入 WRITE\_BEGIN 命令 ACK 超时（App 端 SEQ 错位）

### 现象

* EEPROM 批量写入第一步 `WRITE_BEGIN` 就 ACK 超时/被丢弃，写入流程起不来

### 原因（根因）

* App 端 `writeScoreSession` 里：`request()` 内部自动分配一个 SEQ，但**帧编码用的又是另一个 SEQ**，两者不一致。固件回包的 ACK 携带编码时的 SEQ，App 按自己登记的 SEQ 匹配，永远对不上 → 超时

### 解决

* `GloveLink.request()` 增加可选 `seq` 参数；`writeScoreSession` 为 `WRITE_BEGIN` / `WRITE_DATA` / `WRITE_END` 生成一次 SEQ 并显式传给 `request` 和编码函数，保证登记与发帧同号

### 教训

* 帧的 SEQ 必须**单一来源**：哪个函数编码，就由它决定登记号。任何"编码/登记各算各的"都会造成超时这种看似玄学的问题

***

## 2026-09-01 双手模式下 block1 被误判成发给从机（WRITE\_DATA 目标误判）

### 现象

* 给左手写 EEPROM 时，第一个数据块偶尔被**转发给了右手**，左手写入缺块

### 原因（根因）

* 固件 `handleCommandFrame` 用 `payload[0]` 判断目标设备：`WRITE_DATA` 的**载荷首字节是 blockSeq 而不是 target**，而 block1 恰好 == `TARGET_SLAVE(1)`，被误判成"发给从机"；且写从机时主机会话不激活（BEGIN 仅转发），DATA/END/ABORT 的目标判定全乱

### 解决

* 写命令族（WRITE\_BEGIN/DATA/END/ABORT）的目标设备**统一由 WRITE\_BEGIN 唯一决定**：BEGIN 载荷首字节写进 `lastWriteTarget`，后续 DATA/END/ABORT 一律复用 `lastWriteTarget` 判定，不再看 payload\[0]

### 教训

* 协议解析时**命令的语义不同，载荷字段含义就不同**——`blockSeq` 和 `target` 同字节位置但分属不同命令，绝不能一刀切"读 payload\[0]"。改协议/加命令时列一张「每命令载荷字段表」最稳妥

***

## 2026-09-02 从机恢复后心跳死锁（PONG 永远等不到）

### 现象

* 从机断电再上电，主从链路一直恢复不了：主机 PING 发了但 PONG 不回，miss 无限上涨，`Slave link restored` 永远不上报

### 原因（根因）

* 旧逻辑 `awaitingPong == true` 时**不再发送新 PING**。从机断电期间发的那次 PING 丢失后，`awaitingPong` 一直为 true，后续 PING 全部被吞 → 从机即使恢复也收不到新 PING，死锁

### 解决

* `handleHeartbeat` **每轮都发 PING**：上一轮 PONG 未回就先计 miss 再发新 PING，从机一旦恢复就能立刻收到 PING 回 PONG

### 教训

* 心跳类状态机要写成**闭环可恢复**：任何"等回复期间不发新探测"的逻辑都要警惕死锁；探测要持续发，回复驱动计分，而不是请求驱动计分

***

## 2026-09-02 App 连接时从机已断电 → 断链事件永远错过

### 现象

* 从机在 App 连接**之前**就断了电，App 连接后一直等不到 `Slave link LOST`

### 原因（根因）

* 断链是在 App 连接前发生的，固件当时的检测结果没被重新评估；App 连接后心跳状态还是旧值，从机状态显示"正常"（假正常）

### 解决

* 在 `CMD_HELLO` 处理中**重置心跳链路状态**（见上文 09-01 列表第 2 步），让 App 每次连接都从零开始重新检测从机在线性；同时连接确认后右手保持断电，App 很快收到真实的 `Slave link LOST`

### 教训

* **"状态重置时机"要跟着会话生命周期走**：App (重新)连接 = 新会话，所有链路状态必须复位重测，否则残留旧状态会给出"假正常/假丢失"

***

## 2026-09-02 Windows 侧扫描不到 Glove\_L（手机能看到，电脑扫不到）

### 现象

* 手机能扫到 Glove\_L，但 Windows 上自动化测试重试 8 轮全部失败，列表里只有 HUAWEI M-Pencil 这类无关设备

### 原因（根因）

* Windows BLE 适配器**扫描广播不稳定**：设备多时漏报目标设备；且 `acceptAllDevices: true` 时 Chromium 把无关设备也列为候选，白白占据扫描窗口

### 解决

* 按用户建议把 `requestDevice` 改成 **`filters: [{ namePrefix: 'Glove' }]`**（`src/renderer/src/lib/bluetooth/GloveController.ts`），只匹配手套广播名 → 立刻一次扫描命中

### 教训

* BLE 扫描要尽量**聚焦**：requestDevice 带 name/service 过滤器既能清列表又能提高命中率；Windows 侧"设备在但扫不到"时，优先加过滤器而不是傻等重试

***

## 2026-09-02 测试脚本：Stop 按钮选择器歧义

### 现象

* 自动化点 Stop 时点错按钮（页面上多个含 "Stop" 文本的元素）

### 解决

* 改用精确定位：`locator('button.glove-btn', { hasText: 'Stop' })`

### 教训

* UI 自动化选元素**先限定角色/类名**再匹配文本，避免文本模糊匹配命中多个

***

## 2026-09-02 测试脚本：CRC 断言把 "CRC OK" 误判成失败

### 现象

* 写入明明全部成功（日志带 "CRC OK"），断言却判定失败

### 原因

* 断言正则 `/NAK|CRC|failed|失敗/` 把 "CRC OK" 的 **CRC** 也匹配上，误判失败

### 解决

* 改为只匹配失败形态：`!/NAK|failed|失敗/ && !/CRC\s*(fail|mismatch|err|bad)/`，排除 "CRC OK"

### 教训

* 日志断言别匹配**关键词本身**，要匹配**失败形态的完整表达**，否则正常日志也会误伤

***

## 2026-09-02 Electron 后端 koa-connect 包装导致 ctx 泄漏

### 现象

* App 后端用 koa-connect 包装 Express 中间件后出现 ctx 泄漏/请求错乱

### 原因（根因）

* koa-connect 对 Express 中间件的包装方式不兼容 Koa 上下文模型，包装层引入状态泄漏

### 解决

* **改用原生 Koa 重写**，不再包装 Express 中间件

### 教训

* 中间件体系选型要一致：要么全 Koa 要么全 Express，**用包装层硬塞两种生态**迟早出状态问题

***

## 2026-09-02 从机 SRAM 写完后 WRITE\_RESULT 丢失（半双工总线碰撞）

### 现象

* 同步序列写右手（从机）SRAM：数据块全被 ACK（20/20 已实际写入），但从机的 `WRITE_RESULT(0x89)` 始终收不到，App 侧 `Write failed: WRITE_RESULT timeout`

* 换右手 **EEPROM 330B** 却没这个问题（`Write result: 330/330` 正常返回）——同样是从机，写法不同结果不同

### 原因（根根）

* 主从共用**半双工广播总线**（"SoftwareSerial 广播，各节点按 DST 过滤"），主从的 TX 不能同时发包

* 从机收满字节后 `finishWriteSession()` **立即**回 WRITE\_RESULT；而与此同时，主机刚收到从机的 WRITE\_DATA\_ACK 正在把它**转发给 PC**（约 6ms+ 线时）。两帧在总线上碰撞，WRITE\_RESULT 被冲掉，App 永远收不到

* **为什么 EEPROM 没事**：EEPROM 路径在回 WRITE\_RESULT 前有慢速 I2C 回读校验（几 ms\~几十 ms），天然把两帧错开了；SRAM 路径全是内存操作，快的微秒级，必撞。所以「右手 EEPROM 过、右手 SRAM 挂」是同一根因的两个表现

### 解决

* 从机 `finishWriteSession()` 在发 WRITE\_RESULT 前先 `delay(20)`，等主机转发完 WRITE\_DATA\_ACK、总线静默后再发。实测修复后 `RX seq=0 cmd=0x89` 正常到达 App，右手 SRAM 写 20/20 通过

### 教训

* **半双工总线上的"背靠背回复"要防碰撞**：一个节点收到数据后回 ACK 又紧接着回结果帧时，必须考虑其他节点转发 ACK 的时间窗口。规则的破绽只有在"慢路径掩盖问题、快路径暴露问题"时才会显形（EEPROM 掩 / SRAM 显），写传输类代码默认在连续帧之间留缝隙

***

## 2026-09-02 大乐谱写入失败：WRITE\_END 超时误报（EEPROM 回读校验阻塞）

### 现象

* 用 Summer Time.mxl 全曲（1432 指令 / 2864 字节）写入 EEPROM：前 152 块 WRITE\_DATA 全部 ACK（数据实际已写入），最后一步 `WRITE_END` 报 `ACK timeout after 4 attempts (seq=152)`

* 小乐谱（330B、16B）或 1-2 小节写入全部正常；唯独大乐谱全曲必挂

### 原因（根根）

* 从机收满数据后，`finishWriteSession()` 中要**同步回读整块 EEPROM 逐字节算 CRC 校验**（`eepromReadByte` 走 I2C，2864 字节约需 2\~3 秒）。从机是单线程，此期间主循环被阻塞，无法处理新到的 WRITE\_END → ACK 迟迟不回

* App 端 `WRITE_END` 请求用默认 `ackTimeoutMs=300ms × 4 次重试 ≈ 1.2 秒`，远小于从机校验耗时，必然超时

* **为什么小数据没事**：字节少回读快（330B 约 0.4s），能在超时窗口内返回；数据越大越暴露

### 解决

* `GloveLink.writeScoreSession()` 中 WRITE\_END 改用长超时：`ackTimeoutMs: 8000, maxRetries: 0`（等待从机完成 EEPROM 回读校验，一次收尾不重复轰炸从机）

* 实测全曲 1432 指令写入通过：144 块、2864/2864 字节、CRC OK、0 重发

### 教训

* **协议层"停等"也要区分命令的耗时特性**：写数据块 ACK 快（内存/缓存操作），收尾 ACK 慢（要等持久化校验）。对"慢命令"用统一 300ms 超时必然误报。任何固定超时都要按命令最坏耗时留余量

* 从机若做长阻塞工作（I2C 回读校验、EEPROM 页写），App 侧对应请求必须给足超时，否则会误报失败——即便数据其实已写入成功

***

## 2026-09-02 Stop 等控制命令 ACK 超时误报（总线延迟超出默认窗口）

### 现象

* 综合测试中 Stop / EEPROM play 等控制命令偶发 `ACK timeout`，实际设备已执行成功（无 NAK，播放/停止动作正常）

### 原因（根因）

* 控制命令沿用协议层默认 `ackTimeoutMs=300ms`，但控制帧经主从半双工总线转发：主机调度、从机处理、回 ACK 再转发回 App，全程延迟可超 300ms，ACK 迟到即被误判超时

* 与 WRITE\_END 超时是同类问题：**默认超时窗口是按"主机直连快路径"定的，没考虑转发链慢路径**

### 解决

* `GloveControlPanel/index.tsx` 增加控制命令专用参数 `CTRL_ACK_OPTS = { ackTimeoutMs: 1500, maxRetries: 3 }`，应用于 SRAM play / EEPROM play / Stop / Pause / Resume / Reset

* 实测综合测试（左手×5轮 + 右手写入 + 双手播放）全部通过，Stop/play 命令均为一次 ACK，不再超时误报

### 教训

* 超时参数要**按链路形态分类**：直连命令（HELLO、写数据块 ACK）可以短窗口快速重试；跨从机转发或含阻塞处理的命令（WRITE\_END、控制命令）必须放宽窗口。建议按命令维护一张「超时/重试参数表」而不是全局默认值一刀切

<br />
