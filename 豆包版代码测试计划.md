# 豆包版代码测试计划

> 对应代码路径：`C:\Users\NULL\Documents\key\keyflow - 副本`  
> 配合阅读：《豆包版蓝牙协议理解.md》第 9 节（已知 Bug 列表）  
> 测试前提：两端固件都按豆包版 `trae.ino` 烧录（左手主机、右手从机）。App 端使用 `keyflow - 副本` 目录下的源码构建。

---

## 0. 测试前准备

### 0.1 硬件接线变更（非常重要！⚠️）
**豆包固件将蓝牙从 SoftwareSerial(2/11) 改到了硬件 UART Serial(0/1)**，同时 **SoftwareSerial(8,9) 改成调试口**。接线表：

| 信号 | 旧接线 | 豆包版新接线 |
|---|---|---|
| 蓝牙模组 RX（← Arduino TX） | D11 | **D1 (TX0)** |
| 蓝牙模组 TX（→ Arduino RX） | D2 | **D0 (RX0)** |
| USB-TTL 调试接脚 | 无（原 USB 串口） | **D8=dbg.RX, D9=dbg.TX**（接 USB-TTL 反接：D8→USBTTL.TX, D9→USBTTL.RX）|
| 马达/LED PWM | D3/5/6/9/10 | 不变（注意 D9 仍在马达列表里，和调试口 dbg.TX **引脚冲突**！见 Bug A）|

### 0.2 烧录注意
- **烧录前要把蓝牙模组从 D0/D1 拔掉或断开**，否则 Arduino ISP/USB 串口会被蓝牙占用，烧录失败。烧录完成后再接回。
- 左手：角色默认（`IS_MASTER=true, IS_LEFT=true`）由顶部宏决定。第一次上电后如果 EEPROM 配置块未初始化，会自动写入。之后可用 `CMD_SET_ROLE` 改角色（App UI 暂未暴露，可手动十六进制下发或加临时按钮）。
- 右手：同理。

### 0.3 App 构建
```powershell
cd 'C:\Users\NULL\Documents\key\keyflow - 副本'
npm install           # 若第一次运行
npm run typecheck:web # 已在审查时跑过，可跳过
npm run dev           # 启动 Electron 开发窗口（Web Bluetooth 仅在 dev localhost 可用）
```

### 0.4 USB-TTL 调试监控（强烈建议，能让你看到所有协议细节）
把 USB-TTL 接到 SoftwareSerial(8,9) 引脚上，波特率 **115200**，终端打开。会看到类似：
```
[I] EEPROM found at 0x50
[I] Role cfg: MASTER=Y LEFT=Y
[I] Glove ready: LEFT
[I] Role: MASTER
[I] Sync RTT[0]=2340
...
[I] Sync done bestRtt=1820 offset=120
```

---

## 1. 第一层：静态验证（无需硬件，10 分钟搞定）

### ✅ 1.1 类型检查 + 单测（审查时已全过）
- `npx tsc -p tsconfig.web.json --noEmit` → **0 错误**（已验证）
- `npx vitest run src/renderer/src/lib/bluetooth/protocol/frame.test.ts` → **8/8 通过**（已验证）
- 可选跑完整 vitest：`npx vitest run`（除 file-handlers/generate-licenses 4 例为既有无影响失败外，其余应全过）

### ✅ 1.2 固件编译（Arduino IDE 里做）
- 打开 `keyflow - 副本/trae/trae.ino`，选板 **Arduino Uno**，处理器 **ATmega328P**。
- 点"验证"（✓ 按钮）。**重点看 Flash 和 RAM 占用**：
  - Flash（程序存储）应 ≤ 31KB（32KB 上限，留 1KB 给 bootloader）。如果超限 → 需要把 `DBG_LEVEL` 调低到 2 或 1，或移除 `Dbg_INFO` 级日志。
  - RAM（动态内存）应 ≤ 1.5KB（2KB 上限）。FrameDecoder 缓冲（40B）+ EEPROM 缓存（64B）+ 校时数组（8×4×5=160B）+ 其他全局变量合计需要关注。
- **记录编译结果**：把 Flash/RAM 百分比写到错误记录.md（固件编译成功后）。

### 🔧 预修复（建议在烧录前就做，避免反复）
**Bug A（新发现！比协议瑕疵更急）**：`const int ledPins[] = {3, 5, 6, 9, 10};` 中引脚 9 与 `SoftwareSerial dbg(8, 9);` 的 **dbg.TX 引脚冲突**！D9 同时驱动一个马达 PWM 和 SoftwareSerial 调试输出。会导致该指马达异常乱震 + 调试串口乱码。  
**快速修复选项（二选一）**：
1. 把调试口改成 SoftwareSerial(8, 7)（TX 挪到 D7，D7 没被占用），相应改接线。
2. 如果不需要调试口，把 `DBG_LEVEL` 设为 0（关闭），SoftwareSerial 不会初始化，D9 正常做马达。—— 推荐（量产本来就打算关）。

**Bug B（协议理解 §9 中等问题）**：WRITE_END 收满后 NAK(ERR_STATE)。  
在 `trae.ino handleWriteEnd` 顶部加兼容分支：
```cpp
void handleWriteEnd(uint8_t seq) {
  // 已收满 totalBytes 时 finishWriteSession() 已执行，这里兼容直接 ACK
  if (!wsession.active && wsession.received >= wsession.totalBytes) {
    sendAck(seq, ERR_OK);
    return;
  }
  if (!wsession.active) { sendNak(seq, ERR_STATE); return; }
  ...
}
```

---

## 2. 第二层：单元/协议对拍（无需硬件 App↔模拟设备，30 分钟）

目的：在接线之前验证 App 协议端帧编解码和 CRC 与固件端**字节级一致**。由于两端我已单独审查，已确认一致，但建议做一次跨端对拍。

### 2.1 App↔固件 CRC 一致性对拍
在 `frame.test.ts` 里临时加一条测试，对已知的字节序列计算 CRC16，并与固件打印值对比。  
固件侧临时（烧录后手动触发也行）在 setup 里加：
```cpp
// CRC 对拍
uint8_t test[] = {0x05, 0x01, 0x01, 0x01, 0x00, 0x00}; // 示例：LEN=5,SEQ=01,DST=01,CMD=HELLO,VER=01,CAP=00
uint16_t c = crc16(test, 6);
dbg.print("CRC test: "); dbg.println(c, HEX);
```
App 侧：
```ts
// 在 frame.test.ts 运行一次：
console.log(crc16(Uint8Array.from([0x05,0x01,0x01,0x01,0x00,0x00])).toString(16));
```
**期望两端值一致**（应该是 0xC301 左右的具体数，算出来对比）。

### 2.2 帧转义对拍
取一个包含 `0x7E` 和 `0x7D` 的 payload，两端 encodeFrame 对比字节流。App 侧已测单测 "round-trips a payload containing flag/escape bytes" 全过，固件可用同样数据加临时 dbg 打印对比。

---

## 3. 第三层：硬件最小功能验证（左右手独立，1 小时）

> 每一项通过记 ✅，失败记 ❌+调试日志到「错误记录.md」。

### 3.1 主机左手（不接从机，单手模式）
1. **连接握手**：App 点"连接"，配对后 GloveControlPanel 应立即拿到 `HELLO_ACK`，日志显示 `[连接成功] Device: ...`。USB-TTL 调试口应出现 `EVENT DEVICE_READY` 对应 dbg 输出。
2. **PING**：App 连续 30s 不操作，心跳应正常（ping 不抛错）。
3. **BPM/拍号/强度设置**：
   - BPM 合法：输入 120 → ACK(ERR_OK)；输入 500 → 期望 NAK(ERR_PARAM=2)
   - 强度：指 1 强度 200 → 马达 1 更震
   - 拍号：3/4 → 每小节 3 拍
4. **SRAM 写入 + 播放（32 条简易乐谱）**：
   - 写 SRAM 分区 → 日志显示 WRITE_RESULT bytesWritten=64, crcOk=OK
   - 点"播放 SRAM" → 左手马达震动节奏正确
   - 暂停、继续、停止 三个按钮全部正常
5. **EEPROM 写 + 播放（分区 0）**：
   - 选 8~16 条简单乐谱（避免太多，后面要测极限）
   - 写 EEPROM 分区 0 → 实际字节数 == 预期字节数，CRC=OK
   - 点"播放 EEPROM 分区 0" → 正常
   - 小节跳转：跳 2 → 从第 2 小节开始
   - 清除 EEPROM 分区 0 → 再播放 → 立即停止（空分区）
6. **状态机保护**：进入写入过程中点"播放"→ 期望 NAK(ERR_BUSY=5)。写过程中切模式 → 同样 NAK(ERR_BUSY)。
7. **RESET**：点复位 → 所有参数恢复默认（BPM=120、强度=128）。
8. **SET_ROLE**（可选高级）：手动下发 `SET_ROLE` payload[0]=0x03（主+左）→ 复位 → 角色不变（对照）。下发 payload[0]=0x00 → 复位 → dbg 应该显示 MASTER=N, LEFT=N（然后你得用 ISP/EEPROM 清回 0x03，否则下次就当从机了）。

### 3.2 从机右手（独立测试，把从机的蓝牙模块 C 直连 PC BLE 当"主机"连）
1. 烧豆包固件到右手板子（EEPROM 角色应为 0x00 或 SLAVE 模式，取决于烧录时默认；如果第一块板子是左手，你可以在第一块板的"基础验证"后暂时把右手接入 App 来测独立功能，确认 OK 再接回）。
2. 独立测试步骤**同 3.1 中 2~6**，功能全部等价。

---

## 4. 第四层：双手模式（主从互联，2 小时）

### 4.1 基础链路
1. **校时首次自动触发**：双手模式切换（App → 模式=双手）。
   - 主机自动发起 8 轮校时 → 调试口打印 `Sync RTT[0..7]=xxxx`。
   - App 日志显示 `EVT SYNC_COMPLETE bestRtt=xxxx offset=xxxx`。
   - bestRtt 通常在 2~10ms 之间（38400 波特率下单向 ~2.6ms，合理范围）。
2. **周期重校**：保持 35s 不动 → 调试口应在 30s 点再次打印一轮 Sync RTT。
3. **校时避让写入**：在写 EEPROM 的同时等待 30s 到期 → **校时应被推迟到写会话结束后才发起**（由 `ST_WRITING` 阻止）。

### 4.2 心跳 + 断链
1. **上电，双手正常连接** → 调试口确认 `slaveLinkAlive=true`。
2. **拔掉右手从机电源** → 最多 5+5+5=15s（3 次 miss × 5s 间隔）内应：
   - 调试口输出 `Slave link LOST`
   - App 事件回调收到 `EVT SLAVE_LINK_LOST`（UI 上应有 slaveLinkAlive=false 状态显示）
   - 若正在播放 → 应自动停止
3. **恢复供电** → 1 个心跳周期（5s）内：
   - 调试口 `Slave link restored`
   - App `EVT SLAVE_LINK_RESTORED`
   - **自动重新校时**（timeSynced=false 会触发下一轮 startTimingSync）

### 4.3 双手同步播放（验收标准 4 条）
1. **目测 LED/马达同步**：选一个节拍简单的乐谱（如双手 4 拍一致的节奏），同时播放。
   - 两边马达对应手指**肉眼不可辨延迟**（<5ms，验收标准第 4 条）。
   - 连续播放 3 分钟不累积漂移（NTP 校时 + 周期性重校时机制生效）。
2. **同步控制**：暂停、继续、停止、跳小节，左右手同时生效。
3. **从机断电再恢复**：播放中断电右手 → 主机自动停播 + EVT LOST；上电恢复 → 重新同步后再播放正常。

---

## 5. 第五层：批量写入极限压力测试（解决原始丢包问题的核心验收，1.5 小时）

### 5.1 验收标准 1 复现：164 条乐谱写右手 EEPROM
- **数据准备**：164 条 tick/motor 对 = totalBytes=328。取之前丢包的那份真实数据（你手头那份 164 条）。
- **执行**：连续写入 10 次，每次完整流程（App UI → 目标=右手 → EEPROM → 分区 1，避免分区 0 之前被清空影响）。
- **验收条件（意见书 §4 验收标准 1）**：
  - **10 次全部 bytesWritten=328，crcOk=OK，零人工干预**
  - 每次 `retries` 字段不应超过 5 次（如果高了说明链路有问题，但最终零丢包仍算通过，重传机制生效）
- **日志要求**：每次写入后把 `日志面板完整输出` 保存（特别是 10 次的 retries 分布，可画个表）。

### 5.2 缺口重传验证（模拟丢包）
- 方案 A（易做）：传输过程中快速用手**拔掉右手电源 1 秒后插回** → 刚好丢失一个 WRITE_DATA 块。
  - 期望：固件返回 `WRITE_DATA_ACK[expectedBlock, ERR_GAP=6]`，App 自动重传期望块，最终结果仍 crcOk=OK，bytesWritten 正常。
  - 日志里 `retries` 字段 ≥1。
- 方案 B（精确）：如果方案 A "刚好丢块"难复现，可暂时在固件 `handleWriteData` L1218 手动加一个"如果 blockSeq==5 就强制 GAP 一次"的调试代码来测，测完恢复。

### 5.3 人为干扰（意见书 §4 验收标准 2）
- 写入过程中**快速连续按 "BPM 设置"、"模式切换"、"小节跳转"、"播放"、"停止"** 按钮 → 不允许污染写入。
  - 期望：全部控制命令返回 NAK(ERR_BUSY=5)，写入流程正常推进，最终 bytesWritten/crcOk 无误。
- 写入过程中关闭 App 页面/断开蓝牙重连 → 固件 3s 写会话超时 → `writeSessionAbort ERR_TIMEOUT=8`，回 IDLE 状态，不会卡在 ST_WRITING。

### 5.4 写入取消
- App UI 提供取消按钮（shouldCancelRef 生效）。写一半点取消 → 发送 WRITE_ABORT → 设备中止会话（ERR_ABORT=7），回 IDLE。下次写入正常。

### 5.5 大块（接近 MAX 情况）
- 构造 SRAM 满数据：32 条 × 2 = 64B → 写 SRAM 分区 → 播放正确。
- EEPROM 单分区极限 0x1000=4096 字节，写满 2048 条（1 分区满）→ crcOk=OK。（时间较长，可只做 1 次抽查，不是 10 次重复的那种。）

---

## 6. 第六层：校时精度定量验证（30 分钟）

意见书要求 "LED 同步误差肉眼不可辨（<5ms）"，需要做一次定量验证。

### 6.1 方案 A：debug 口打时间戳
双手同时触发第一个马达时，用一个小的示波器/逻辑分析仪探头同时接左手 D3 和右手 D3（同一个指的马达 PWM），看第一个上升沿的时间差。
- **如果没有示波器**：临时在固件 `schedulePlayback` / `eepromSchedule` 的 `executeCommand` 前一行加 dbg 打印 `micros()`（只有第一次触发时打印，避免刷屏）。
  ```cpp
  // 在 executeCommand 开头加入
  static bool firstCmd = true;
  if (firstCmd) {
    dbg.print("[EXEC] us=");
    dbg.println(micros());
    firstCmd = false;
  }
  // startPlayback 时 firstCmd = false;
  ```
- 播放一条"第 1 tick 左右两手同时触发同一指"的测试乐谱。对比主机 dbg 打印的时间和从机 dbg 打印的时间（**注意两边板子都是独立 micros()，但校时后 offset 已知**，换算后对齐）。差值 ≤ 5ms 通过。

### 6.2 方案 B：反复 N 次取差值
重复启动/停止播放 20 次，每次记录 8 轮 bestRtt，观察 RTT 分布稳定性：
- bestRtt ≤ 10ms 占比 ≥ 80%
- offset 标准差 ≤ ±500µs
（要求不高，因为 RTT 本身 2-6ms，offset 计算精度在微秒级的 1/2 RTT 是几百 µs 级）

---

## 7. 第七层：8MHz 自制板前导测试（可选，为未来铺路，30 分钟）

意见书 §4 验收标准 6："固件在 8MHz 硬件 UART 配置下全部功能等价可用"。

当前你用的是 16MHz 晶振的 UNO，先做一次**软件层面的 8MHz 兼容性验证**（不需要换晶振，改 fuse 也行但更麻烦）：
1. 在 Arduino IDE 里把 `Tools → Board → Arduino Uno` → 选 `Processor: ATmega328P (3.3V, 8MHz)` 选项（如果没这个选项，装 `ATmegaCore` 板包）。
2. 改完后**编译通过**即可（就算是通过 8MHz 配置验证）。实际 8MHz 板焊好后再跑一遍第 3~5 层全量用例。

---

## 8. 回归/兼容测试（可选，30 分钟）

如果以后你打算做 **旧 App ↔ 新固件** 或 **新 App ↔ 旧固件** 混合使用的兼容性验证（按意见书 §5 "不需要向后兼容"，其实不需要做），豆包声称保留了旧 `0x40/0x41` 控制分包（`handleLegacyControlPacket`），可以用旧 App 发一次"播放"命令验证从机响应是否正确。—— 一般不用做，直接全量上新协议。

---

## 9. 报告方式 / 通过判定

### 9.1 每轮完整测试后生成测试记录（建议写进 错误记录.md 或单独 txt）
```
日期：
固件：左手/右手 豆包版（hash/时间戳）
App：keyflow - 副本（commit/时间戳）
接线：D0/D1 蓝牙 / D8/D7 调试（或 DBG_LEVEL=0）

P1 固件编译 Flash=__% RAM=__%    [PASS/FAIL]
P2 App typecheck + vitest         [PASS/FAIL]
P3 左手独立功能（3.1 8 条）        [__/8 PASS]
P4 右手独立功能（3.2 6 条）        [__/6 PASS]
P5 双手校时 + 心跳 + 断链          [__/3 PASS]
P6 双手同步播放肉眼误差 <5ms       [PASS/FAIL]
P7 164 条写右手 ×10 次 crcOk=OK    [__/10 PASS，每次 retries: ___]
P8 缺口重传                        [PASS/FAIL]
P9 人为干扰不污染写入              [PASS/FAIL]
P10 写入取消 + 超时回滚            [PASS/FAIL]
P11 校时精度定量                   [差值 = __µs PASS/FAIL]

备注/失败项：
```

### 9.2 通过判定
- 第 3/4/5 层（基础/双手/批量写入）**100% PASS** 才能投入实际使用。
- 已知瑕疵（协议理解 §9 瑕疵 1-6）**不影响功能的可以先过**，Bug A/B 必须修复才能算通过。
- 若 164 条 10 次中有任何 1 次 crcOk=FAIL / bytesWritten≠预期，需要**追因**（重传机制失效？UART 冲突？），修好后重跑。

---

## 10. 速查：测试时常用调试命令列表

**App 侧 Chrome DevTools Console**：
- 所有 `encoded` 字节流可以 `console.log([...encoded].map(b=>('0'+b.toString(16)).slice(-2)).join(' '))` 转十六进制看。
- GloveLink 的日志可以临时在 `feedBytes` / `handleFrame` 里加 `console.log('RX frame', frame)` 查看原始帧。
- 写过程进度：`onProgress(written, total)` 每块触发一次，可用来评估块级传输速率（~20 块/秒左右算正常）。

**固件 USB-TTL（115200）**：
- `[I] Sync RTT[i]=xx` 校时每轮 RTT。
- `[I] Sync done bestRtt=xx offset=xx` 校时结果。
- `[W] Slavelink LOST`、`[I] Slave link restored` 心跳。
- `[W] Write session timeout, abort` 写超时。
- 想要更详细的帧级日志，把 `DBG_LEVEL` 调到 3 后加一行 `DBG_INFO` 在 `decoder.frameReady` 时打印 seq/dst/cmd/payloadLen。

---

*祝你好运：按这个流程走一遍，大概半天到 1 天能跑完。核心要抓的是 Bug A 的引脚冲突和 Bug B 的 WRITE_END 流程——修完这俩，后面用例通过率会很高。*
