# 豆包版蓝牙协议理解（基于代码审查）

> 版本：豆包重构版（v2，HDLC 风格帧）  
> 代码范围：`trae/trae.ino` + `src/renderer/src/lib/bluetooth/`（路径：`C:\Users\NULL\Documents\key\keyflow - 副本`）  
> 对照基准：《蓝牙协议重构意见.md》第 3 节「新协议设计建议」

---

## 0. 总体评价（一句话）

**豆包实现了重构意见书中 16 条问题中 14 条的解决方案，核心机制（HDLC 帧、ACK/重传、批量写会话、8 轮 NTP 校时、心跳、硬件 UART、EEPROM ACK 轮询、显式状态机、角色 EEPROM 可配）均已落地，基本没有曲解需求；存在 4 处小的实现瑕疵（§9 列出），需要修复后再烧录验证。**

---

## 1. 系统拓扑与数据流

```
           BLE (GATT)
PC (App) ────────────────► 蓝牙模块 A（从角色，UART 透传）
                               │ 硬件 UART (0/1 脚)，38400 + U2X
                               ▼
                     ┌── Arduino UNO 主机（左手）──┐
                     │   Serial = 蓝牙（硬件 UART） │
                     │   SoftwareSerial(8,9) = 调试 │
                     └─────────────┬────────────────┘
                                   │ 同一 Serial 共线广播
                                   ▼
                     蓝牙模块 B（主角色）—— BLE 无线 ——► 模块 C ——► Arduino 从机（右手）
```

**关键隔离机制（豆包新增 DST 字段）**：主机 TX 一条 Serial 线同时接到模块 A（回 PC）和模块 B（去从机），每帧带 `DST` 字段：
- `DST = 0x01 (DST_PC)`：只有 App/Notify 链路消费；从机丢弃。
- `DST = 0x02 (DST_SLAVE)`：从机处理；App 侧 FrameParser 丢弃。

解决了意见书 P6「目标设备为隐式全局状态」问题的链路层部分。

---

## 2. 帧格式（与意见书 3.1 一致）

```
┌──────┬──────┬──────┬──────┬──────┬───────────────────┬────────────┬────────────┐
│ 0x7E │ LEN  │ SEQ  │ DST  │ CMD  │ PAYLOAD (0..25B)  │ CRC16 L    │ CRC16 H    │
└──────┴──────┴──────┴──────┴──────┴───────────────────┴────────────┴────────────┘
  FLAG   (1)   (1)    (1)    (1)     (LEN-3 = 0..25)      小端（低字节在前）
```

### 字段含义
| 字段 | 说明 |
|---|---|
| FLAG `0x7E` | 帧起始标志；帧内永不出现（被字节填充转义）。 |
| LEN | `= 3 + PAYLOAD 长度` = SEQ+DST+CMD+PAYLOAD 总字节（不含 LEN 自身和 CRC），范围 `3..28`。 |
| SEQ | 请求方自增序号（0~255 回绕），响应帧回传同 SEQ 用于匹配；未匹配 ACK 时 300ms 超时重发（最多 3 次）。 |
| DST | `0x01=DST_PC`（发往 PC/主机链路）、`0x02=DST_SLAVE`（发往从机链路）。**注意：这里是链路层路由，不是 payload[0] 的 target 目标设备位。** |
| CMD | 命令码；`<0x80`=命令段（请求），`≥0x80`=响应/事件段。 |
| PAYLOAD | 小端字节序（与 AVR/ARM 原生一致）；最大 25 字节（未转义帧 ≤32）。 |
| CRC16 | **CRC-16/CCITT-FALSE**，多项式 `0x1021`，初值 `0xFFFF`，无反射、无异或输出。覆盖范围 **LEN..PAYLOAD（即含 LEN 字段在内的 `LEN+1` 字节）**，结果小端写入。 |

### 字节填充（转义）
帧内（LEN..CRC 整个 body）出现 `0x7E` 或 `0x7D` 时：
| 原始字节 | 传输序列 |
|---|---|
| `0x7E` FLAG | `0x7D 0x5E`（ESC + ESC_FLAG） |
| `0x7D` ESC  | `0x7D 0x5D`（ESC + ESC_ESC） |

### ⚠️ CRC 覆盖范围（豆包的"修正点"）
重构意见书 3.1 原文写的是 "CRC 覆盖 LEN~PAYLOAD"，豆包把 **LEN 字段本身也纳入 CRC 计算**，并在 App `frame.ts:94` 和固件 `trae.ino:347` 同步。这属于对意见书语义的**合理解读**（LEN 是帧 body 的第一个字节），**两端实现一致**，没有不一致问题。

---

## 3. 命令集与响应模型

### 3.1 命令码空间（方向判定）
```
0x01~0x3F  命令段（App→设备 / 主机→从机）
0x80~0xBF  响应/事件段（设备→App / 从机→主机）
```

### 3.2 命令表（App → 主机，payload 首字节常为 target 目标位）

| 命令 | 码 | payload | 说明 |
|---|---|---|---|
| HELLO | `0x01` | `[protoVer, capability]` | 连接握手；协议版本协商。 |
| PING | `0x05` | — | 心跳。 |
| STOP | `0x10` | `[target]` | 停止；双手模式下主机发未来动作 + 从机同步执行。 |
| PAUSE | `0x11` | `[target]` | 暂停。 |
| RESUME | `0x12` | `[target]` | 继续。 |
| RESET | `0x13` | `[target]` | 系统复位（清空状态 + 缓存）。 |
| SRAM_PLAY | `0x14` | `[target]` | 播放 SRAM 内容。 |
| EEPROM_PLAY | `0x15` | `[target, partition]` | 播放 EEPROM 分区（0~7）。 |
| CLEAR_SRAM | `0x16` | `[target]` | 清空 SRAM 缓冲区。 |
| CLEAR_EEPROM | `0x17` | `[target, partition]` | 在分区头写结束标志 `FF 00`。 |
| JUMP_BAR | `0x18` | `[target, bar]` | 跳转到指定小节（bar 从 1 起）。 |
| SET_MODE | `0x19` | `[target, mode]` | `0=单手 1=双手`；切双手时自动触发校时。 |
| SET_BPM | `0x1A` | `[target, bpm:2 LE]` | 范围 1~300；越界 → NAK ERR_PARAM。 |
| SET_TIME_SIG | `0x1B` | `[target, beats]` | 拍号分子（分母 UI-only）。 |
| SET_TPS | `0x1C` | `[target, tpsCode]` | `0=32 1=16 2=8`（每拍 tick 分辨率）。 |
| SET_INTENSITY | `0x1D` | `[target, pin, value]` | pin=0~4（5 指），value=PWM 强度。 |
| WRITE_BEGIN | `0x20` | `[target, storage, partition, totalBytes:2 LE, batchCrc:2 LE]` | 开启批量写会话。 |
| WRITE_DATA | `0x21` | `[blockSeq, data:0..20]` | 数据块；blockSeq 从 0 起。 |
| WRITE_END | `0x22` | — | 结束会话；触发设备回 WRITE_RESULT。 |
| WRITE_ABORT | `0x23` | — | 取消会话。 |
| SYNC_REQ | `0x30` | `[txn, t1:4 LE]` | 主机→从机校时请求（App 不发）。 |
| SYNC_OFFSET | `0x31` | `[txn, offset:4 LE]` | 主机→从机下发时间偏移。 |
| SET_ROLE | `0x32` | `[roleByte]` | 写 EEPROM 配置角色后软复位。 |

> 目标位 `target`：`0=TARGET_LOCAL（本地=左手=主机）`，`1=TARGET_SLAVE（右手=从机）`。  
> 存储介质 `storage`：`0=STORAGE_SRAM`，`1=STORAGE_EEPROM`。

### 3.3 响应 / 事件表（主机/从机 → App）

| 响应 | 码 | payload | 对应 |
|---|---|---|---|
| HELLO_ACK | `0x81` | `[protoVer, capability, fwVer:2 LE, role]` | HELLO |
| ACK | `0x82` | `[status]` | 通用成功；`status=ERR_OK(0)` 或其他。 |
| NAK | `0x83` | `[errCode]` | 通用失败。 |
| PONG | `0x86` | `[statusFlags]` | PING 响应；flags=设备状态枚举值。 |
| EVENT | `0x87` | `[eventCode, data...]` | 设备主动上报（见下表）。 |
| WRITE_DATA_ACK | `0x88` | `[blockSeq, status]` | 逐块 ACK；GAP(ERR_GAP=6) 让 App 重发期望块。 |
| WRITE_RESULT | `0x89` | `[bytesWritten:2 LE, crcOk, status]` | 批量写最终结果；设备回读校验。 |
| SYNC_REP | `0xB0` | `[txn, t1:4, t2:4, t3:4]` | 从机→主机；8 轮 NTP 中间结果。 |
| SYNC_ACK | `0xB1` | `[txn, status]` | 从机 offset 应用确认。 |

### 3.4 事件码（EVENT payload[0]）

| 事件 | 码 | payload 后续 |
|---|---|---|
| DEVICE_READY | `0x01` | — |
| SLAVE_LINK_LOST | `0x02` | —（主机 3 次心跳未中 → 停播放 + 上报） |
| SLAVE_LINK_RESTORED | `0x03` | —（收到 PONG/从机帧 → 恢复） |
| SYNC_COMPLETE | `0x05` | `[bestRtt:2 LE, offset:4 LE]`（8 轮中 RTT 最小的一轮） |

### 3.5 错误码（NAK / status 语义统一）
| 码 | 常量 | 含义 |
|---|---|---|
| 0 | ERR_OK | 成功 |
| 1 | ERR_CRC | 校验失败 |
| 2 | ERR_PARAM | 参数越界 |
| 3 | ERR_STATE | 状态不允许（非法状态下收到该命令） |
| 4 | ERR_STORAGE | 存储失败 |
| 5 | ERR_BUSY | 忙，如写会话中收到非写入命令 |
| 6 | ERR_GAP | 块序号缺口，请求重发期望块 |
| 7 | ERR_ABORT | 会话被主动取消 |
| 8 | ERR_TIMEOUT | 会话超时 |

---

## 4. 请求/响应模型（对应意见书 3.2）

- 每条命令必须产生一个响应（ACK 或 NAK 或命令专属响应，如 HELLO→HELLO_ACK、WRITE_DATA→WRITE_DATA_ACK）。
- 响应帧的 **SEQ = 命令帧的 SEQ**（固件 `sendAck(seq, status)` 保持 seq）。
- App 侧 `GloveLink.requestWithSeq`：
  - 发送后登记 pending（按 SEQ 映射）；
  - 300ms 超时 → 重发，最多 3 次 → 失败。
- GloveControlPanel 层把 throw 包成 `{ok:boolean, error, errorCode}`，UI 友好。

**⚠️ 实现瑕疵 1**：`GloveLink.ping()` 走 request 但 heartbeat 定时 5s 没重置 heartbeatMiss；`handleHeartbeat` 发送 PING 后若从机不回 PONG，App 端 **pingFailCount 只在 PONG 收到时清零**，但发送端 `handleHeartbeat` 在固件里没有 increment `heartbeatMiss`——实际是在主循环 `checkSlaveLink` 依赖固件侧 `heartbeatMiss` 计数器累加。但 **App→主机 PING 不经过 `handleHeartbeat`**，App 的 PING（GloveController.connect 后心跳）是**发给主机自己**（DST_PC），不是发给从机的。这是两条独立心跳，功能没问题但设计略绕，不属于 bug。

---

## 5. 批量写入会话（对应意见书 3.3）

```
App                         主机 / 从机
 │  WRITE_BEGIN [T,S,P,total,batchCrc]  →  准备分区、清缓存、置 ST_WRITING
 │  ← ACK (ERR_OK) / NAK (ERR_BUSY / ERR_PARAM)
 │
 │  WRITE_DATA [blockSeq=0, data≤20B]  →
 │  ← WRITE_DATA_ACK [0, ERR_OK]
 │  WRITE_DATA [blockSeq=1...]          （停等，间隙同步刷 EEPROM 页）
 │     ...缺口检测...
 │  ← WRITE_DATA_ACK [expectedBlock, ERR_GAP]  →  App 重发该期望块
 │     ...
 │  WRITE_END                            →
 │  ← ACK + (设备主动回) WRITE_RESULT [bytesWritten, crcOk, status]
 ```

### 关键实现点
- **WRITE_BEGIN.payload** 7 字节：`[target, storage, partition, totalBytes:2, batchCrc:2]`；校验参数后进入 ST_WRITING，EEPROM 模式下先在分区头写 `FF 00` 结束标志，SRAM 模式下 clearScore。
- **WRITE_DATA**：payload[0]=blockSeq，其余为乐谱字节（每块 ≤20，实际 ≤10 条 tick/motor 对）。**`expectedBlock` 不匹配立即 ERR_GAP 回期望序号**，不推进接收指针。
- **EEPROM 非阻塞写入**：64B 缓存 `eepromCache`，累积到满/跨页时 `flushEepromCache`；`eepromWritePage` 内部 **ACK 轮询**（不是旧版 delay(5)），单次 ≤10ms。
- **收满后 CRC 回读验证**（豆包新增，意见书 3.3 第 3 点要求）：从 EEPROM 回读 `bytesWritten` 字节重新计算 CRC16，与 WRITE_BEGIN 声明的 `batchCrc` 对比，结果写入 WRITE_RESULT.crcOk。
- **会话超时**：3s 无新数据 → `writeSessionAbort(ERR_TIMEOUT)`，发 NAK + 回 ST_IDLE。
- **取消**：App 发 WRITE_ABORT → `writeSessionAbort(ERR_ABORT)`。

**⚠️ 实现瑕疵 2（重要）**：`finishWriteSession()`（`trae.ino:1242`）在 `handleWriteData` 收满 totalBytes 时**主动**被调用，此时已经向 PC 发了 WRITE_RESULT（`seq=0`，未匹配任何 pending）。之后 App 再发 WRITE_END → `handleWriteEnd` 判 `wsession.active=false` → NAK(ERR_STATE)。App 端的 `writeScoreSession`（`GloveLink.ts:409`）对 WRITE_END 等待 ACK，此时会收到 NAK 抛异常并进一步调用 **WRITE_ABORT**，流程多余但**不影响数据正确性**（WRITE_RESULT 已经缓存且 crcOk 已校验）。建议在 WRITE_END 处理时兼容 `wsession.active==false && received>=totalBytes` 的情况，直接 ACK。

**⚠️ 实现瑕疵 3（SRAM 写右手）**：`handleCommandFrame` 处理 WRITE_BEGIN/WRITE_DATA/WRITE_END/WRITE_ABORT 时 `toSlave == TARGET_SLAVE` 就转发。如果 `storage=SRAM && target=SLAVE`，从机侧 `beginWriteSession` 也走 SRAM 分支——这是对的，功能 ok。但 App 端 payload target=`TGT_SLAVE` 时 dst=`DST_SLAVE` 也正确。**唯一注意：`totalBytes > MAX_CMDS * 2` 的校验（trae.ino:1179）对 SRAM 生效，MAX_CMDS=32 → totalBytes ≤ 64，和意见书一致。**

---

## 6. 校时协议（8 轮 NTP，对应意见书 3.4）

主机为时钟参考，从机为校准方。

```
主机                              从机
 ─ SYNC_REQ [txn, t1:4 LE] ────►  记录 t2 = 本地 micros()
                                    记录 t3 = micros()（发回时刻）
 ◄── SYNC_REP [txn, t1, t2, t3] ──
 记录 t4 = 本地 micros()
 RTT[i] = (t4-t1) - (t3-t2)   ← 剔除对端处理耗时
 重复 8 轮（i=0..7）
 选 i=best 使 RTT 最小
 offset = ((t2[best]-t1[best]) + (t3[best]-t4[best])) / 2
 ─ SYNC_OFFSET [txn, offset:4 LE] ──► 从机 timeOffset = offset
 ◄── SYNC_ACK [txn, ERR_OK] ──
 主机上报 EVENT SYNC_COMPLETE [bestRtt:2, offset:4] → App
```

- `offset = 从机本地时钟 − 主机估计`。从机 `playbackAnchorTime()` 执行 `micros() - timeOffset` 对齐主时钟。
- 事务号 `txn` 每次校时会话自增 1，陈旧 SYNC_REP 被丢弃（`handleSyncRep` L1029）。
- 采样间隔 `delay(10)` 轮间延迟（L1077），避免 UART 总线过于饱和。
- **周期重校**：30s（`SYNC_INTERVAL`）；写会话期间 `devState=ST_WRITING` 不发起校时（意见书 3.4 要求）。
- 单轮等待超时 2s（`SYNC_WAIT_TIMEOUT_US`）→ `continueTimingSync` 重发本轮。

**⚠️ 实现瑕疵 4（非致命但值得修）**：`handleSyncReq` 中 `t2 = micros(); t3 = micros();` 两行连续调用，两者差值为 0（或几 us），实际「对端处理耗时」信息可以忽略，功能不受影响但略不严谨（意见书设计 `t2` 应是接收时刻、`t3` 是发送时刻，中间应有处理，但此处没有处理所以相等，不影响计算）。

---

## 7. 心跳与断链检测（对应意见书 3.5）

- **主机→从机心跳**：`HEARTBEAT_INTERVAL_US = 5s`，空闲时主机发 `PING(DST_SLAVE)`；从机回 `PONG(DST_PC)`。
- **断链判定**：`heartbeatMiss ≥ 3` 且 `slaveLinkAlive == true` → `stopPlayback()` + 上报 `EVENT SLAVE_LINK_LOST`。
- **恢复判定**：下次收到 PONG / 从机任何帧 → `heartbeatMiss = 0`，上报 `EVENT SLAVE_LINK_RESTORED`。
- **App↔主机心跳**：GloveLink 自己也有 5s PING(DST_PC)，目的是检测 App↔主机 BLE 链路健康；连续 3 次无 PONG 只是停心跳定时器（当前实现不触发 UI 告警），功能 ok 但告警可以后续加。

---

## 8. 固件端基础设施

### 8.1 硬件 UART 迁移（意见书 3.7 第 1 条）
- 蓝牙走 **硬件 UART Serial（0/1 脚）**，`38400` + `UCSR0A |= (1<<U2X0)`（U2X 倍速）：
  - 16MHz：U2X 38400 误差 0.2%（理想）
  - 8MHz：U2X 38400 误差 0.16%（意见书验证的安全值）
- **调试口让位**：SoftwareSerial(8,9) + `DBG_LEVEL` 宏，量产设为 0 编译掉所有 `DBG_*`。

### 8.2 接收状态机（意见书 3.7 第 2 条）
`FrameDecoder` 结构体：主循环 `Serial.available()` 逐字节 feed；状态 `idle → collecting`，支持 LEN 预知、转义、坏帧 CRC 丢帧后立即回 idle。任何位置丢字节最坏只丢一帧（意见书 P1 目标达成）。

### 8.3 显式状态机（意见书 3.7 最后一条）
```
ST_IDLE / ST_WRITING / ST_PLAYING / ST_PAUSED / ST_SYNCING
```
`handleCommandFrame` 开头：写会话中只接受 WRITE_DATA/WRITE_END/WRITE_ABORT，其他一律 NAK(ERR_BUSY)。播放命令在非 IDLE 状态下的保护：由各命令分支中的状态检查实现（部分命令如 JUMP_BAR 会在非播放态下 NAK）。

### 8.4 角色 EEPROM 可配置（意见书 3.7 倒数第 2 条）
- AT24C256 高端配置块基址 `0x7F00`：MAGIC `K`(0x7F00)/`F`(0x7F01)，`0x7F02` role，`0x7F03` version(=1)。
- roleByte：bit0=主/从（1=主），bit1=左/右（1=左）。
- `CMD_SET_ROLE`：写配置 → delay(50) → `resetFunc()` 跳 0 软复位。

### 8.5 双手同步播放控制（旧 0x40 机制的兼容保留）
主机侧 `sendControlData` 仍用 `0x40/0x41` 指令（新帧格式封装内），从机 `handleLegacyControlPacket` 解析未来动作时刻 → 叠加 offset → scheduleFutureAction。播放/暂停/继续/停止/SRAM 播放/EEPROM 播放共 6 个动作都走这条路径。这不是"旧协议兼容"，而是把意见书 2.5 节播放同步需求**内嵌到帧格式里**，做法合理。

---

## 9. 审查发现的问题清单（按严重度排序）

### 🟡 中等问题（建议修复后烧录验证）

**Bug 1：WRITE_END 收满后可能收到 NAK(ERR_STATE)**  
位置：`trae.ino:1242` → `handleWriteData` 收满时直接 `finishWriteSession()`，内部置 `wsession.active=false`。随后 App 按流程发 WRITE_END → L1289 判 active=false → NAK(ERR_STATE)。App 端 GloveLink 对 WRITE_END 等 ACK 失败抛错，再发 WRITE_ABORT。  
**数据不丢**（WRITE_RESULT 已经发送且 App 缓存），但流程体验差，日志报"error"。  
**修复建议**：`handleWriteEnd` 增加 `wsession.received >= wsession.totalBytes && !wsession.active` 的兼容分支 → 直接 ACK。

**Bug 2：SYNC_REQ 中 t3 与 t2 相等，语义不对**（极低影响）  
位置：`trae.ino:1089-1090`。虽然对最终 RTT 计算几乎无影响（处理耗时确实≈0），但更严谨做法是 t3 在发送 SYNC_REP 前再取一次。

### 🟢 轻度问题 / 代码风格（可后续修）

**瑕疵 3：handleCommandFrame 中 forwardToSlave 的判定对双手模式有冗余**  
`(IS_MASTER && !dualMode && toSlave)` 对于 STOP/PAUSE 等命令：当 `dualMode=true` 时即使 `toSlave` 也不转发，而是走"未来动作 + 同步执行"路径。这是正确的（双手模式下控制总是通过未来时刻同步，而非单纯转发），但代码注释不明显，需要理解上下文才能看懂。

**瑕疵 4：WRITE_RESULT 的 seq=0，不通过 pending 匹配**  
`finishWriteSession` `sendFrame(DST_PC, CMD_WRITE_RESULT, ..., 0)` 使用 seq=0。App 端是通过缓存 `lastWriteResult` + waiter 队列来消费。功能 ok 但略绕（意见书 3.3 要求 WRITE_END 后回复 WRITE_RESULT，豆包设计为提前主动发，再用 waiter 等，功能等价）。

**瑕疵 5：App 心跳失败仅停定时器，不上报 UI**  
`pingFailCount >= pingFailThreshold` 时只 `stopPing()`，UI 层没有断开告警。GloveControlPanel.connect 有 HELLO 超时重传，BLE 层有 GATT disconnect 事件，但 App↔主机侧协议层心跳断链检测不完整。实际影响很小（BLE 层断链会自然发现）。

**瑕疵 6：glove-constants.ts 与旧代码共享 UUID，无问题**  
SERVICE=0xfff0 / WRITE=0xfff2 / NOTIFY=0xfff1，和 MX-02P 规格书一致（规格书第 4 节：WRITE 0xfff2, NOTIFY/INDICATE 0xfff1）。✅

### ✅ 已确认无问题的核心点

1. CRC16 实现：App 端 crc16(TS) ↔ 固件端 crc16(cpp)：两端 **多项式/初值/无反射** 完全一致，且都覆盖 LEN 字段。✅  
2. 帧格式两端一致（LEN=3+payloadLen，DST/SEQ/CMD 顺序）。✅  
3. 批量写入 `totalBytes` 声明 + `batchCrc` 校验：App `writeScoreSession` 的 `data.length` 和 `batchCrc = crc16(data)` 与固件 `finishWriteSession` EEPROM 回读 CRC 对齐（注意：SRAM 的 CRC 计算固件侧从 cmdTicks/cmdMotors 数组逐个拼，等价于 App 侧 data 顺序，需要实际对拍验证）。✅  
4. 8 轮 NTP 选 RTT 最小、删除 8000µs 魔法常数：完成。✅  
5. 结束标志 `FF 00` 在 WRITE_BEGIN 时预写（EEPROM）和 `totalBytes` 声明，不再走旧协议的"最后一条特殊指令"机制。App 端 `ScoreConverter` 输出不含结束标志。✅  
6. 字节序：除了 `Serial.write` 流（字节流本身无大小端概念），所有 16/32 位字段全部**小端**。✅  
7. EEPROM ACK 轮询替代 delay(5)：完成。✅  
8. 调试分级宏（DBG_LEVEL）：完成。✅  
9. 固件编译大小（估算）：ATmega328P 32KB Flash，62699 字节 .ino 会是挑战——实际编译后 Flash 占用需要 Arduino IDE 验证。⚠️ 需用户实际编译确认。

---

## 10. 与《蓝牙协议重构意见》的对照

| 意见书问题 | 豆包是否解决 | 实现位置 |
|---|---|---|
| P1 帧定界/错位 | ✅ HDLC 0x7E + 字节填充 | FrameDecoder/FrameParser |
| P2 序列号/确认/重传 | ✅ SEQ + ACK/NAK + 300ms×3 次 | GloveLink.request |
| P3 半双工碰撞 | ⚠️ 部分（硬件 UART 减少关中断、块协议降低转发频次 10x、写会话屏蔽校时）。主从共线广播仍存在。意见书建议的"收发分离"未实现（受限于 UNO 硬件资源） | setup() Serial + beginWriteSession 清校时 |
| P4 校验形同虚设 | ✅ 全帧 CRC16，WRITE_RESULT 回读校验 | crc16 + finishWriteSession |
| P5 批量传输低效 | ✅ 20B 块协议，168 次 → ~20 次 BLE 写 | writeScoreSession |
| P6 隐式 scoreTarget | ✅ 帧 DST 路由 + payload target 位 + WRITE_BEGIN 会话内声明 | commands + frame |
| P7 设备→App 无反馈 | ✅ Notify 特征 + ACK/NAK/WRITE_RESULT/EVENT | attach(notifyChar) + handleNotify |
| P8 字节序不统一 | ✅ 全部小端 | frame/commands payload 组装 |
| P9 32 位值拆帧 | ✅ 变长帧一帧装 4 字节 | SYNC_REQ payload |
| P10 校时粗糙 | ✅ 8 轮 NTP + bestRtt 选最小 | continueTimingSync |
| P11 帧布局不一致 | ✅ 统一 HDLC 变长帧 | encodeFrame |
| P12 无协议版本 | ✅ PROTOCOL_VERSION=1 + HELLO 握手（能力位未用，保留 capability=0x00） | buildHello + HELLO_ACK |
| P13 无心跳/存活 | ✅ 主机→从机 5s PING，3miss 断链；App↔主机独立 PING | handleHeartbeat |
| P14 阻塞 EEPROM 写 | ✅ ACK 轮询替代 delay(5) | eepromWritePage |
| P15 角色写死 | ✅ EEPROM 0x7F00 配置 + SET_ROLE 指令 | readRoleConfig |
| P16 清除分区语义不清 | ⚠️ 仍然只写结束标志（原意见书允许"功能可用"，豆包未实现真清零。不影响功能，命名仍有误导） | CLEAR_EEPROM 分支 |

---

## 11. 端到端时序图（写右手乐谱 + 播放 + 校时）

```
App                        主机（左）                        从机（右）
 │ HELLO[ver=1,cap=0]          │                                 │
 │──────────────────────────►  │                                 │
 │  HELLO_ACK[ver=1,role=3]    │                                 │
 │◄──────────────────────────  │                                 │
 │  EVT DEVICE_READY           │                                 │
 │◄──────────────────────────  │  （30s 到，启动校时）             │
 │                             │  SYNC_REQ[txn=1,t1]             │
 │                             │────────────────────────────────►│
 │                             │     SYNC_REP[txn,t1,t2,t3]      │
 │                             │◄────────────────────────────────│
 │                             │       ...（7 轮重复）...         │
 │  EVT SYNC_COMPLETE[rt,off]  │                                 │
 │◄──────────────────────────  │                                 │
 │                             │                                 │
 │ WRITE_BEGIN[T=1,E=1,P=0,    │                                 │
 │   total=328,crc=xxxx]       │                                 │
 │──────────────────────────►  │  WRITE_BEGIN (转发 DST_SLAVE)   │
 │                             │────────────────────────────────►│
 │                             │          ACK[seq,ERR_OK]        │
 │  ACK[seq,ERR_OK]            │◄────────────────────────────────│
 │◄──────────────────────────  │                                 │
 │ WRITE_DATA[0,20B...]        │  WRITE_DATA (转发 DST_SLAVE)    │
 │──────────────────────────►  │────────────────────────────────►│
 │                             │    WRITE_DATA_ACK[0,ERR_OK]     │
 │  WRITE_DATA_ACK[0,ERR_OK]   │◄────────────────────────────────│
 │◄──────────────────────────  │                                 │
 │      ...（16 块 x 20B + 1 x 8B）...                           │
 │ WRITE_END                   │  WRITE_END (转发 DST_SLAVE)     │
 │──────────────────────────►  │────────────────────────────────►│
 │                             │  ACK + WRITE_RESULT[328,ok,0]    │
 │  ACK / WRITE_RESULT 缓存    │◄────────────────────────────────│
 │◄──────────────────────────  │                                 │
 │ EEPROM_PLAY[T=1,P=0]        │                                 │
 │──────────────────────────►  │  set future action + send 0x40  │
 │                             │────────────────────────────────►│
 │                             │                                 │ (500ms 后同步触发)
 │ ACK[ERR_OK]                 │  ▶ startPlayback                │ ▶ startPlayback
 │◄──────────────────────────  │                                 │
```

---

*文档生成时间：基于豆包版代码审查完成（trae.ino 1921 行 / App protocol 模块 4 核心文件 / GloveControlPanel 调用链）。测试计划见《豆包版代码测试计划.md》。*
