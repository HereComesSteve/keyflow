# Keyflow 蓝牙协议重构意见书

> 本文档目的：现有蓝牙协议是项目早期非专业设计，已在实际使用中暴露大量问题（丢包、帧错位、状态失步、无法定位错误）。本文档逐条列出现有协议缺陷，并给出**以效果最优为目标**的重构设计建议，不限定改动难度与工作量。供重构实施者（人或 AI）作为完整需求输入。
>
> 涉及代码：固件 `trae/trae.ino`、App 端 `src/renderer/src/lib/bluetooth/`（GloveController.ts、glove-commands.ts、glove-constants.ts）、`src/renderer/src/components/GloveControlPanel/index.tsx`。

***

## 一、系统现状概述（重构前必读）

### 1.1 硬件拓扑

```
PC (Web Bluetooth)
   │  BLE（GATT Write 特征 0000fff2）
   ▼
蓝牙模块 A（从角色，UART 透传）
   │  UART（SoftwareSerial，引脚 2/11，当前 38400 波特率）
   ▼
Arduino UNO 主机（左手，ATmega328P 16MHz）
   │  UART（同一个 SoftwareSerial 实例，半双工）
   ▼
蓝牙模块 B（主角色，绑定模块 A）
   │  BLE 无线
   ▼
蓝牙模块 C → Arduino UNO 从机（右手）
```

关键约束：

- PC 只与主机（左手）建立 BLE 连接；右手的一切数据必须由主机经 **同一个 SoftwareSerial** 转发（收和发共用一个半双工软串口，这是丢包主因）。

- ATmega328P 每侧只有 1 个硬件 UART（已被 USB 调试占用），蓝牙走 SoftwareSerial。

- 未来可能自制 8MHz 无外部晶振开发板：SoftwareSerial 在 8MHz 下最高只稳定到 19200；**重构时应把蓝牙挪到硬件 UART（0/1 脚），USB 调试改用 SoftwareSerial 或直接取消**。硬件 UART 在 8MHz 下开 U2X 跑 38400 误差仅 0.16%，完全可靠。

### 1.2 现有协议帧格式

固定 4 字节帧，按首字节分三类：

| 首字节    | 用途     | 格式                        | 校验                                      |
| ------ | ------ | ------------------------- | --------------------------------------- |
| `0xF9` | 控制指令   | `F9 [cmd] [arg] [00 或数据]` | 无                                       |
| `0xFA` | BPM 设置 | `FA [高8] [低8] 00`（大端）     | 无                                       |
| `0xFB` | 乐谱数据   | `FB [tick] [motor] [XOR]` | XOR（**且固件默认关闭**，`enableXorCheck=false`） |

控制指令子命令（0xF9 的 cmd 字节）：

| cmd         | 功能            | 备注                         |
| ----------- | ------------- | -------------------------- |
| 00/01/02/03 | 停止/暂停/继续/复位   | 双手模式下拆成 0x40/41/42 时间戳分包转发 |
| 04          | SRAM 播放       | <br />                     |
| 05          | EEPROM 分区播放   | arg=分区 0\~7                |
| 10          | 清空 SRAM       | <br />                     |
| 11          | 进入 EEPROM 写模式 | arg=分区                     |
| 12          | 清除 EEPROM 分区  | 实际只写结束标志                   |
| 20          | 模式切换          | arg: 0=单手 1=双手             |
| 21          | 拍号            | arg=beats<<4               |
| 22          | 每拍 tick 分辨率   | <br />                     |
| 24          | 目标设备切换        | arg: 0=左手 1=右手（**全局隐式状态**） |
| 25          | 马达强度          | 第 4 字节是数据（与其他指令布局不一致）      |
| 26          | 小节跳转          | <br />                     |
| 30\~37      | 校时握手          | 见 2.5                      |
| 40/41/42    | 控制指令时间戳分包     | 32 位时刻拆 3 帧                |

乐谱写入流程：`F9 24 [目标]` → `F9 12 [分区]`（清）→ `F9 11 [分区]`（进入写模式）→ N × `FB [tick] [motor] [XOR]` → 结束标志 `FB FF 00 04`。

### 1.3 现有校时握手（0x30\~0x37）

主机发 `0x30`（记 T\_send）→ 从机回 `0x31` + 两帧 `0x32/0x33`（T\_recv 拆 2 帧，小端）→ 主机算 `offset = T_recv - (T_send + RTT/2) + 8000us补偿`，用 `0x34/0x35` 两帧下发 offset → 从机回 `0x36` 确认。每 30 秒重校一次。

***

## 二、现有协议问题清单（按严重程度）

### 2.1 致命问题

**P1. 无帧定界保障，丢 1 字节即帧错位，且错位会连锁扩散。**
接收端靠"扫描 F9/FA/FB 同步头 + 固定 4 字节长度"重组帧。一旦链路丢 1 字节，后续所有字节整体错位：下一条指令的同步头被当作上一条的数据，直到偶然再撞上同步头才重新对齐。而乐谱数据中 tick/motor 字节**可以等于 0xF9/0xFA/0xFB**，错位后会锁定到假同步头上，造成"错误帧看起来格式合法"。实测 164 条丢 12 条，正是"丢字节→错位→XOR 校验失败丢弃→吞掉相邻帧"的连锁反应。
**行业做法**：HDLC 式标志字节 + 字节填充（转义），或 COBS 编码，保证数据区永不出现标志字节，实现 O(1) 重同步。

**P2. 无序列号、无确认、无重传。**
所有指令发出去就不管了（App 用 `writeValueWithoutResponse`，固件收多少算多少）。丢帧无法检测、无法恢复，只能靠用户肉眼对比 `Bytes written`。乐谱数据 168 帧任何一帧丢失都导致 EEPROM 内容残缺，且**结束标志本身也可能丢**——丢了就永远停在写模式（只能靠 2 秒超时兜底）。
**行业做法**：块级序列号 + ACK + 超时重传（简化版 XMODEM），或至少整批校验 + 一次性重发。

**P3. 主机转发链路半双工碰撞（当前丢包的物理根源）。**
主机用同一个 SoftwareSerial 既收 PC 数据又转发给从机。`bt.write()` 位敲击期间关闭中断，9600 时每条 4 字节关中断 4.2ms，此窗口内 PC 到达的字节直接丢失。提速到 38400 后窗口降为 1.05ms（碰撞率 \~2%），但**架构性缺陷仍在**。
**行业做法**：收发分离（硬件 UART 收 + 独立引脚发），或转发前确认接收缓冲已排空，或从根本上改为"块传输 + 停等协议"让收发天然交替。

### 2.2 严重问题

**P4. 校验形同虚设。**
唯一的 XOR 校验只覆盖 0xFB 乐谱帧，且固件默认 `enableXorCheck=false`；所有控制指令零校验。XOR 本身也只能检奇数位错误，检不出字节交换、丢字节错位。
**行业做法**：全帧统一 CRC-16（如 CRC-16/CCITT），默认强制开启。

**P5. 批量传输效率极低。**
乐谱数据每 4 字节 BLE 写入只携带 2 字节有效数据（50% 开销），且每帧一次独立 BLE 写（ATT 头 3 字节 + L2CAP 开销）。164 条 = 168 次 BLE 写 × 50ms 人为间隔 ≈ 8.4 秒。BLE 单次写入可携带 ~~20 字节（默认 MTU 23），利用率不到 20%。
**行业做法**：按 MTU 组块（每块 16~~20 字节乐谱数据 + 块头 + CRC），168 帧压缩到 \~20 个块，传输时间和碰撞面都缩小一个数量级。

**P6. 隐式全局状态** **`scoreTarget`（F9 24）引发竞态。**
"目标设备"是全局状态而非每条指令的属性。传输中途任何误发（模式切换会把它重置为左手）、丢帧、顺序错乱都会让数据写错手。已实际发生过"左手乐谱被清空"类事故。
**行业做法**：每帧/每事务显式携带目标地址字段；或引入事务 ID，写入会话开始时声明目标+总量，期间不可被其他指令打断。

**P7. 设备→App 完全没有反馈通道。**
固件只用了 Write 特征，没有 Notify/Indicate 特征。设备无法向 App 报告：写入完成、实际字节数、校验失败、电量、错误码。所有验收靠 USB 串口线看打印。
**行业做法**：增加 Notify 特征作为统一的设备→App 响应/事件通道，所有命令都有响应帧（成功/失败/错误码）。

**P8. 字节序不统一。**
BPM 用大端（`FA [高8] [低8]`），校时 T\_recv/offset/时间戳用小端拆帧。实现和排查都容易出错。
**行业做法**：协议层统一小端（与 ARM/AVR 原生一致）。

### 2.3 一般问题

**P9. 32 位值拆帧传输无原子性。**
T\_recv、offset、执行时刻都拆成 2~~3 个独立帧传输，中间丢任何一帧，状态机卡死直到 3~~5 秒超时，且**没有事务标识**——上一轮握手的迟到帧可能污染下一轮。
**行业做法**：变长帧，一个值装在一帧里；握手帧带事务序号。

**P10. 校时算法粗糙。**
单次 RTT 采样、`RTT/2` 对称假设、8000us 魔法常数补偿。BLE 链路延迟抖动可达毫秒级，单采样误差大。
**行业做法**：NTP 式多次采样（如 8 轮），取 RTT 最小的一轮计算 offset；或连续滤波（指数加权）。

**P11. 帧布局不一致。**
`F9 25`（强度）第 4 字节是数据，其他指令第 4 字节是 00 或校验；结束标志 `FB FF 00 04` 的校验字节 04 是硬编码特例。解析端靠一堆 if 分支硬编码。
**行业做法**：统一帧结构 `[同步][长度][命令][载荷][CRC]`，一张表驱动解析。

**P12. 无协议版本、无连接握手。**
App 和固件版本不匹配时行为不可预期，无法协商能力。
**行业做法**：连接后先交换 HELLO（协议版本号、能力位、固件版本）。

**P13. 无链路心跳/存活监测。**
断连只能靠 BLE 层事件发现；主从之间无线断连后从机状态（主机维护的推断值）永久失真。
**行业做法**：周期心跳帧 + 超时判定，断连自动停止播放并复位状态。

**P14. 固件侧阻塞操作污染接收。**
EEPROM 页写后 `delay(5)` 硬等待、大量 `Serial.print` 调试输出都在主循环里阻塞，阻塞期间 SoftwareSerial 接收缓冲（64 字节）可能溢出。
**行业做法**：EEPROM 改用 ACK 轮询（写入后发起空传输轮询直到 ACK，非阻塞）；调试打印用编译开关分级，量产关闭。

**P15. 角色编译期写死。**
`IS_LEFT`/`IS_MASTER` 靠改常量重新烧录切换，容易烧错。
**行业做法**：上电读取 EEPROM 配置字节或引脚电平决定角色，或提供配置指令。

**P16. "清除分区"语义不实。**
`F9 12` 只在分区头写结束标志，不清数据。功能上可用（播放读到标志即停），但命名误导，且残留数据无法审计。

***

## 三、新协议设计建议（目标方案）

### 3.1 帧格式（统一、变长、自定界）

采用 HDLC 风格：

```
[0x7E] [LEN:1] [SEQ:1] [CMD:1] [PAYLOAD:0..N] [CRC16:2 小端]
```

- `0x7E` 标志字节；载荷中出现的 `0x7E` 和转义符 `0x7D` 用字节填充（`0x7D 0x5E` / `0x7D 0x5D`）转义，**数据区永不出现假标志**，任何位置丢字节后最坏丢一帧即可重同步；

- `LEN` = SEQ+CMD+PAYLOAD 长度，接收端可提前发现长度异常；

- `SEQ` 帧序号（0\~255 回绕），用于 ACK 匹配与丢帧检测；

- `CRC16`（CRC-16/CCITT-FALSE，多项式 0x1021）覆盖 LEN\~PAYLOAD；

- 最大帧长建议 ≤ 32 字节（UART 侧）；BLE 侧按协商 MTU 组块。

### 3.2 命令/响应模型

- 每条命令帧必须产生一个响应帧：`CMD_ACK`（携带原 SEQ + 状态码）或 `CMD_NAK`（携带错误码）；

- App 端对关键命令（写入会话、播放控制）等待 ACK，超时（如 300ms）重发，最多 3 次；

- 新增 Notify 特征承载所有设备→App 帧（响应、事件、心跳），Write 特征只承载 App→设备帧；

- 错误码表统一定义：`0=成功, 1=校验失败, 2=参数越界, 3=状态不允许, 4=存储失败, 5=忙, ...`

### 3.3 批量写入协议（乐谱传输，替代逐条 0xFB）

写入会话五步：

1. `WRITE_BEGIN`：载荷 = \[目标设备:1]\[存储介质:1]\[分区:1]\[总字节数:2]\[整批CRC16:2]。设备回复 ACK 并进入接收态（此时停止播放、屏蔽校时）；
2. `WRITE_DATA`：载荷 = \[块序号:1]\[数据:≤20字节]。每块独立 CRC + 独立 ACK；设备按块序号检测缺口，缺口即 NAK 请求重发该块（停等或滑动窗口 2\~4 均可，建议先做停等，简单可靠）；
3. 设备收满 `总字节数` 后校验整批 CRC16；
4. `WRITE_END`：设备回复实际写入字节数 + 整批校验结果；不符则 App 整体重传；
5. 会话超时（如 3 秒无数据）设备自动放弃写模式并回复错误。

收益：164 条乐谱从 168 次 BLE 写降到 \~20 次；任何位置丢包都能精确定位并重传；结束标志不再可能静默丢失（总长度预先声明）。

### 3.4 校时协议重构

- 单帧携带完整时间戳（变长帧容得下 4 字节），不再拆帧；

- 握手帧带事务序号，防陈旧帧污染；

- 连续 8 轮 `SYNC_REQ/SYNC_REP`，主机记录每轮 (T\_send, T\_recv, T\_back)，**取 RTT 最小的一轮**计算 offset（NTP 标准做法），删除魔法常数补偿；

- 校时与数据传输互斥：写入会话期间禁止发起校时（现行代码已临时实现，重构后作为协议规则固化）；

- 播放同步改为"主机下发绝对播放时刻 + 双方按校时偏移换算本地时刻"，保留现有 0x40 类机制但并入统一帧格式。

### 3.5 链路层规则

- 心跳：空闲 5 秒发一次 `PING/PONG`（携带各自运行状态位：播放中/写模式/电量）；连续 3 次无响应判定断链，从机自动停止播放；

- 主机↔从机转发改为"收完整帧→校验通过→再转发"，转发前确认本端接收缓冲已排空，天然规避半双工碰撞；配合块协议后转发频次降低 10 倍，碰撞面进一步缩小；

- 目标设备不再是全局状态：写入会话在 `WRITE_BEGIN` 中声明，播放/控制指令帧内携带目标位。

### 3.6 BLE 层建议

- 连接后协商 MTU（Web Bluetooth 不可控，按默认 23 字节设计，预留更大 MTU 的代码路径）；

- 关键控制命令可用 writeWithResponse，批量数据用 writeWithoutResponse + 协议层 ACK（链路层已有可靠性，协议层 ACK 主要解决设备侧处理失败与缓冲溢出）；

- App 端删除固定 50ms 发送间隔，改为"发一块→等 ACK→发下一块"的自然流控。

### 3.7 固件实现建议

- **蓝牙改用硬件 UART**（为 8MHz 自制板铺路；8MHz + U2X 跑 38400 误差 0.16%）；USB 调试口让位或取消；

- 接收用环形缓冲 + 主循环解帧，解析与执行分离；

- EEPROM 写入去掉 `delay(5)`，改 ACK 轮询非阻塞等待；

- 调试打印分级宏（`DBG_ERROR/WARN/INFO`），量产构建全部编译掉；

- 角色/左右手改为上电读 EEPROM 配置字节（提供一条配置指令或烧录时写入），不再改代码重烧；

- 状态机显式化：IDLE / WRITING / PLAYING / PAUSED / SYNCING，非法状态收到指令一律 NAK+错误码，不再静默忽略。

### 3.8 App 端建议

- 协议层独立成模块（帧编解码、重传、会话管理），与 UI 完全解耦；

- 所有操作有明确的结果反馈（成功/失败/重试次数），写入进度按块显示；

- 保留手动十六进制输入作为底层调试入口，但默认走结构化表单（音符→指令由 ScoreConverter 生成后直接进批量写入会话）。

***

## 四、验收标准（重构完成后应满足）

1. 164 条乐谱写入右手，连续 10 次 `实际字节数 == 预期字节数`，零人工干预；
2. 人为制造干扰（传输中快速按其他按钮、切换模式）不会污染写入会话，设备始终能 NAK 或超时自恢复；
3. 拔掉从机电源再上电，主机 15 秒内检测到断链并报告，恢复后自动重新校时；
4. 校时精度：双手 LED 同步误差肉眼不可辨（<5ms），日志输出 8 轮采样的 RTT 分布；
5. 全部指令有 ACK/NAK 响应，App 日志可追溯每条指令的最终结果；
6. 固件在 8MHz 硬件 UART 配置下（未来自制板）全部功能等价可用。

***

## 五、过渡说明

- 当前代码库已在旧协议上做了临时缓解（38400 波特率、校时避让、转发计数诊断），这些是**临时措施**，重构后应全部被新协议机制取代；

- 旧协议指令表见本文档 1.2 节，重构期间如需对照行为可参考 `trae/trae.ino` 的 `parseCommand` 与 `src/renderer/src/lib/bluetooth/glove-commands.ts`；

- 重构范围包含固件与 App 两端，必须同步切换，无兼容层（设备与 App 同属一个项目，不需要向后兼容旧版本）。

<br />
