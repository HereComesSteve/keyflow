# 蓝牙协议重构说明

> 对应需求：《蓝牙协议重构意见.md》
> 范围：PC 桌面端（Electron/TS）↔ 主机（左手，Arduino UNO + AT24C256）↔ 从机（右手）
> 状态：App 端类型检查通过；固件待用户侧 Arduino IDE 编译验证

---

## 1. 帧格式（HDLC 风格变长帧）

```
┌──────┬──────┬──────┬──────┬──────┬──────────────────────┬────────────┬────────────┐
│ 0x7E │ LEN  │ SEQ  │ DST  │ CMD  │ PAYLOAD (0..25)      │ CRC16 L    │ CRC16 H    │
└──────┴──────┴──────┴──────┴──────┴──────────────────────┴────────────┴────────────┘
```

- **LEN** = SEQ+DST+CMD+PAYLOAD 的字节数（不含 LEN 与 CRC），`3..28`。
- **SEQ**：请求方自增序号，用于请求/响应匹配与重发去重（App 侧分配）。
- **DST**：`0x01`=PC，`0x02`=从机（仅主机 UART 转发用；PC 直连主机时恒为 `0x01`）。
- **CRC16**：CCITT-FALSE（多项式 `0x1021`，初值 `0xFFFF`，无反射），**覆盖 LEN..PAYLOAD 整个 body（含 LEN 字段）**，结果小端（低字节在前）写入帧尾。
  - ⚠️ 布局陷阱：LEN 字段本身占用 1 字节，CRC 覆盖起点是 `[0]`、终点是 `[LEN]`（共 `LEN+1` 字节），CRC 字节位于 `[LEN+1]`、`[LEN+2]`。若按“从 SEQ 起算”会整体错位 1 字节（本重构已修正，App `frame.ts` 与固件 `trae.ino` 同步修正）。
- **未转义帧长 ≤ 32 字节**；`MAX_PAYLOAD = 25`。

### 字节填充（转义）

| 原始字节 | 传输序列 |
|---|---|
| `0x7E` (FLAG) | `0x7D 0x5E` (ESC + ESC_FLAG) |
| `0x7D` (ESC) | `0x7D 0x5D` (ESC + ESC_ESC) |

- `0x7E` 只作帧首，帧内不出现；`0x7D` 只作转义引导符，其后必须跟 `0x5E`/`0x5D`。
- 解码状态机：`idle → collecting`，LEN 预知帧长，CRC 失败即整帧丢弃并回 `idle`（可抵抗 BLE 丢字节/丢包）。

---

## 2. 命令 / 响应 / 事件 / 错误码

**分段约定：命令 < `0x80`；响应/事件 ≥ `0x80`。**

### 命令（PC → 主机；`payload` 首字节常为目标位 `target`）

| 命令 | 码 | payload |
|---|---|---|
| HELLO | `0x01` | `[protoVer][capability]` |
| PING | `0x05` | — |
| STOP | `0x10` | `[target]` |
| PAUSE | `0x11` | `[target]` |
| RESUME | `0x12` | `[target]` |
| RESET | `0x13` | `[target]` |
| SRAM_PLAY | `0x14` | `[target]` |
| EEPROM_PLAY | `0x15` | `[target][partition]` |
| CLEAR_SRAM | `0x16` | `[target]` |
| CLEAR_EEPROM | `0x17` | `[target][partition]` |
| JUMP_BAR | `0x18` | `[target][bar]`（1 起） |
| SET_MODE | `0x19` | `[target][mode]`（0=单 1=双） |
| SET_BPM | `0x1A` | `[target][bpm:2 LE]` |
| SET_TIME_SIG | `0x1B` | `[target][beats]` |
| SET_TPS | `0x1C` | `[target][tpsCode]`（0=32 1=16 2=8） |
| SET_INTENSITY | `0x1D` | `[target][pin][value]` |
| WRITE_BEGIN | `0x20` | `[target][storage][partition][totalBytes:2 LE][batchCrc:2 LE]` |
| WRITE_DATA | `0x21` | `[blockSeq][data:0..20]` |
| WRITE_END | `0x22` | — |
| WRITE_ABORT | `0x23` | — |
| SYNC_REQ | `0x30` | `[txn][t1:4 LE]`（主机→从机） |
| SYNC_OFFSET | `0x31` | `[txn][offset:4 LE]`（主机→从机） |
| SET_ROLE | `0x32` | `[roleByte]`（写 EEPROM 后软复位） |

### 响应 / 事件（主机/从机 → PC）

| 响应 | 码 | payload |
|---|---|---|
| HELLO_ACK | `0x81` | `[protoVer][capability][fwVer:2 LE][role]` |
| ACK | `0x82` | `[status]` |
| NAK | `0x83` | `[errCode]` |
| PONG | `0x86` | `[statusFlags]` |
| EVENT | `0x87` | `[eventCode][data...]` |
| WRITE_DATA_ACK | `0x88` | `[blockSeq][status]` |
| WRITE_RESULT | `0x89` | `[bytesWritten:2 LE][crcOk][status]` |
| SYNC_REP | `0xB0` | `[txn][t1:4][t2:4][t3:4]`（从机→主机） |
| SYNC_ACK | `0xB1` | `[txn][status]`（从机→主机） |

### 事件码（EVENT payload 首字节）

| 事件 | 码 | 说明 |
|---|---|---|
| DEVICE_READY | `0x01` | 设备就绪 |
| SLAVE_LINK_LOST | `0x02` | 从机链路丢失（主机主动上报） |
| SLAVE_LINK_RESTORED | `0x03` | 从机链路恢复 |
| SYNC_COMPLETE | `0x05` | 校时完成 `[bestRtt:2 LE][offset:4 LE]` |

### 错误码（NAK / WRITE_DATA_ACK / WRITE_RESULT 的 status）

| 码 | 含义 |
|---|---|
| 0 | ERR_OK |
| 1 | ERR_CRC（校验失败） |
| 2 | ERR_PARAM（参数越界） |
| 3 | ERR_STATE（状态不允许） |
| 4 | ERR_STORAGE（存储失败） |
| 5 | ERR_BUSY（忙） |
| 6 | ERR_GAP（块序号缺口） |
| 7 | ERR_ABORT（会话被取消） |
| 8 | ERR_TIMEOUT（会话超时） |

---

## 3. 批量写入会话（App ↔ 主机）

解决旧协议“逐条指令、无校验、写 EEPROM 前需手工进写入模式”的问题：

```
App                         主机
 │  WRITE_BEGIN[totalBytes][batchCrc]  →  ┐ 准备分区、清缓存
 │  ← ACK                                ┘
 │  WRITE_DATA[blockSeq=0][data≤20]  →   ┐ 停等
 │  ← WRITE_DATA_ACK[blockSeq][status]   ┘ ERR_GAP→重发当前块
 │  WRITE_DATA[blockSeq=1]...            （逐块，块间同步刷 EEPROM 缓存）
 │  WRITE_END                        →
 │  ← WRITE_RESULT[bytesWritten][crcOk][status]   （设备主动回）
```

- **CRC 语义**：`batchCrc` 为整批原始数据的 CRC16/CCITT-FALSE（App 端 `ScoreConverter` 输出不含结束标志，长度由 `WRITE_BEGIN.totalBytes` 预声明）。
- **非阻塞写**：设备端 64B 缓存，在块停等间隙同步刷写；页写 `eepromWritePage` 保证不跨 64B 页边界、单次 ≤30B，并用 **ACK 轮询** 替代固定 `delay(5)`。
- **取消**：App 发起 `WRITE_ABORT`；超时/CRC 失败回滚并提示。
- App 侧 `GloveLink.writeScoreSession` 负责：分块、停等、GAP 重发、`WRITE_RESULT` 等待（缓存 + waiter 队列）、超时/取消路径。

---

## 4. 校时（8 轮 NTP，替代 8000µs 魔法常数）

旧固件以 `8000µs` 固定“主机→从机播放延迟”，与晶振漂移、负载无关，误差累积。

新协议（主机为时钟参考，从机为校准方）：

```
主机                              从机
 SYNC_REQ[txn][t1]  ──────────────→  记录 t2 = 本地 micros()
                                    SYNC_REP[txn][t1][t2][t3]  (t3=发送时刻)
 ←──────────────────────────────
 记录 t4 = 本地 micros()
 RTT = (t4 - t1) - (t3 - t2)          （剔除对端处理耗时）
 重复 8 轮，选 RTT 最小轮：
 offset = ((t2 - t1) + (t3 - t4)) / 2
 SYNC_OFFSET[txn][offset]  ────────→  记录 offset
                                    SYNC_ACK[txn][status]
```

- **offset 定义 = 从机本地时钟 − 主机估计**：从机播放锚点 `playbackAnchorTime` 统一减去 `offset`，使双手以主机时钟对齐触发。
- **事务序号 `txn`** 防止陈旧 SYNC_REP 混入下一轮。
- 完成后主机向 PC 上报 `EVENT[SYNC_COMPLETE][bestRtt][offset]`。
- 周期 `SYNC_INTERVAL = 30s` 重新校时（上电首次进 `ST_SYNCING`）。

---

## 5. 心跳断链检测

- 主机空闲 5s（`HEARTBEAT_INTERVAL_US`）发 `PING` 到从机；从机回 `PONG`。
- 连续 3 次（`HEARTBEAT_MAX_MISS`）未收到 `PONG` → 判定链路丢失：
  - 停播放，向 PC 上报 `EVENT[SLAVE_LINK_LOST]`；
  - 后续收到 `PONG`/从机帧 → 上报 `EVENT[SLAVE_LINK_RESTORED]`。
- PC 端 `GloveController` 通过 `setEventCallback` 将事件映射到 UI（`slaveLinkAlive` 状态 pill / 日志）。

---

## 6. 硬件 UART 与调试口让位

- 蓝牙模组改用**硬件 UART**（UNO 的 0/1 引脚，`Serial`，38400 + U2X 倍速），为 8MHz 自制板时序铺路；USB 串口（USB-TTL 转接）不再占用。
- 调试输出走 `SoftwareSerial(8,9)`，由 `DBG_LEVEL` 宏编译开关（`0=关 1=ERROR 2=WARN 3=INFO`；量产置 `0` 全部编译掉）。
- ⚠️ 上电前需确认蓝牙模组 RX 接 UNO TX(1)、TX 接 UNO RX(0)，与调试口物理隔离。

---

## 7. 设备状态机（显式）

```
ST_IDLE ──WRITE_BEGIN──▶ ST_WRITING ──WRITE_END/ABORT/完成──▶ ST_IDLE
   ▲                          │
   └──────────────────────────┘
ST_IDLE ──SRAM/EEPROM_PLAY──▶ ST_PLAYING ──PAUSE──▶ ST_PAUSED
   ▲                           │  ▲                  │
   └────STOP/播放结束───────────┘  └──RESUME──────────┘
ST_IDLE ──(上电)──▶ ST_SYNCING ──SYNC_OFFSET 应用──▶ ST_IDLE（30s 周期复入）
```

- 命令按当前状态校验（如 `ST_WRITING` 时禁止播放类命令 → `ERR_STATE`）。
- 从机侧另有 `SlaveState` 由主机维护（IDLE/RECEIVING/READY/PLAYING/PAUSED/ERROR）。

---

## 8. 角色 EEPROM 可配置（不再改常量重烧）

- 上电读 AT24C256 高端配置块：
  - `0x7F00` `'K'`、`0x7F01` `'F'`（MAGIC），`0x7F02` ROLE，`0x7F03` VERSION(=1)；
  - 非法/未初始化 → 写入默认角色后进入。
- `CMD_SET_ROLE[roleByte]`：App 下发 → 主机写 EEPROM → `delay(50)` → 软复位（复位后按新角色运行）。
- roleByte 编码：bit0=主/从（1=主），bit1=左/右（1=左）。具体取值以固件 `SET_ROLE` 分支为准（已实现 `IS_MASTER`/`IS_LEFT` 双布尔恢复）。

---

## 9. 兼容层

- 固件保留旧 `0x40`/`0x41` 控制分包（从机侧 `handleLegacyControlPacket`），使旧 App/旧固件组合不彻底断链。
- 新 App 不再发送任何旧 4 字节指令（`glove-commands.ts` 中 `PLAYBACK_COMMANDS` 等已全部移除），仅保留 `toHexString`/`parseScoreInput`/容量与参数常量 + `WRITE_BLOCK_SIZE=20`。

---

## 10. App 侧实现结构

```
src/renderer/src/lib/bluetooth/
├── protocol/
│   ├── frame.ts       帧编解码：crc16 / encodeFrame / FrameParser / parseFramesStream
│   ├── commands.ts    协议常量（CMD/响应/事件/错误码/目标/存储）+ 命令组装函数
│   └── GloveLink.ts   链路层：BLE 分片写、请求/响应(SEQ 匹配+超时重传)、
│                      writeScoreSession(批量写)、心跳 PING/PONG
├── GloveController.ts 连接流程(Write+Notify)、HELLO 握手、sendCommand 高层入口
├── glove-constants.ts BLE UUID（SERVICE 0xfff0 / NOTIFY 0xfff1 / WRITE 0xfff2）
└── glove-commands.ts  纯工具 + 容量/参数常量（旧指令构建已移除）
```

- 请求/响应：`sendCommand` 分配 SEQ → `encodeFrame` → 20B/片串行写 → 按 SEQ 匹配响应，默认 300ms×3 次超时重传。
- 批量写：`writeScoreSession`（BEGIN→DATA 停等→GAP 重发→END→`WRITE_RESULT`），失败发 `WRITE_ABORT`。
- 接收：`handleNotify` 只处理 `DST_PC` 且 `≥0x80` 的帧。

---

## 11. 验证状态

| 项 | 状态 |
|---|---|
| App 类型检查（typecheck:web / node） | ✅ 通过 |
| frame 协议单测（编解码/转义/分片/CRC/重同步，8 例） | ✅ 通过 |
| ScoreConverter 单测（26 例，移除结束标志后） | ✅ 通过 |
| 完整 vitest 套件 | 除 file-handlers(3)/generate-licenses(1) 为既有环境失败外全通过 |
| 固件编译验证 | ⏳ 需用户侧 Arduino IDE / arduino-cli 验证（本机无可用工具链） |
| 上电联调（HELLO 握手 / 批量写 / 校时 / 心跳 / 角色切换） | ⏳ 需硬件联调 |

### 本轮新增/修正
- 修正 **帧 CRC 布局偏移**（App `frame.ts` + 固件 `trae.ino` 同步）：CRC 覆盖含 LEN 字段的 `LEN+1` 字节，CRC 字节位于 `[LEN+1]/[LEN+2]`。
- 新增 `frame.test.ts`（8 例，覆盖转义往返、分片重组、多帧连发、CRC 损坏恢复）。
- i18n 三语（zh/en/ja）补齐 `slaveLinkAlive/slaveLinkLost/slaveLinkRestored/syncComplete/logWriteResult/logWriteErrorCrc`（ja 为类型正源，已与 en 对齐）。
- `ScoreConverter.test.ts` 同步移除 `FF 00` 结束标志断言。

### 第二轮修复（硬件级 Bug A + 协议层 Bug B）
- **Bug A（D9 冲突，硬件级）**：`ledPins[]={3,5,6,9,10}` 的 D9（第 4 指马达 PWM）与 `SoftwareSerial dbg(8,9)` 的 TX 撞脚 → 马达乱震 + 调试口乱码。
  - 修复：`DBG_LEVEL=0`，且将 `SoftwareSerial dbg` 对象声明纳入 `#if DBG_LEVEL>=1`，并把固件中 **6 组裸 `dbg.print` 调用**（setSlaveState / readRoleConfig / handleSyncRep×2 / applyOffset / setup 尾部）全部包裹进 `#if DBG_LEVEL>=3`。
  - 否则仅改 `DBG_LEVEL` 不够：裸调用在 `dbg.begin()` 被编译掉后仍会执行 SoftwareSerial::write → 仍写 D9。
  - 验证：模拟 `DBG_LEVEL=0` 预处理 → 无真实 `dbg.` 代码引用残留；`DBG_LEVEL=3` → 调试代码完整（19 处引用、11 组 `#if/#endif` 配对、单层嵌套）。
- **Bug B（WRITE_END 收满被 NAK）**：`handleWriteData` 收满 `totalBytes` 即调 `finishWriteSession()`（置 `wsession.active=false` 并回 WRITE_RESULT），App 随后发 WRITE_END 被判 `!active` → NAK(ERR_STATE)，UI 误报 error（数据不丢）。
  - 修复：`handleWriteEnd` 顶部加兼容分支——`!active && received >= totalBytes` 时直接 `sendAck(ERR_OK)`（`finishWriteSession` 不清零 `received`，条件成立）。
- **SYNC_REQ t3==t2（不修，无功能影响）**：从机 `handleSyncReq` 中 `t2=micros(); t3=micros();` 紧贴，`t3-t2≈0` → RTT 公式 `(t4-t1)-(t3-t2)` 中该项≈0，RTT 即主机往返，数学自洽。仅当从机处理耗时不可忽略时才有偏差，当前无此情形；无干净修复方案（严格化需在 `sendFrame` 内部打 t3，侵入过大），保持现状。

### 第三轮修复（编译错误）
- **`crc16` 未声明（C++ 类作用域）**：`FrameDecoder::push` 在类内内联定义中调用 `crc16`，而 C++ 要求被调函数在类定义点已可见。原 `crc16` 前向声明位于 `FrameDecoder` 结构体之后，导致 `error: 'crc16' was not declared in this scope`。
  - 修复：将 `uint16_t crc16(const uint8_t *data, uint16_t len);` 前向声明移至 `struct FrameDecoder` 之前（现 286 行），并移除"函数声明"块中的重复声明。
  - 已排查：全文件仅 `FrameDecoder` 与 `WriteSession` 两个结构体，后者为纯数据成员无此问题。
