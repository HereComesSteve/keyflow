# Keyflow 硬件测试报告（蓝牙协议重构版 v2）

> 测试时间：2026-09-02
> 测试对象：左手主机（Glove\_L，Arduino UNO 主机）＋ 右手从机（Glove\_R）
> 测试方式：Playwright E2E 驱动 Electron App（Web Bluetooth）＋ 固件串口调试日志对照（DBG\_LEVEL=3，115200）

***

## 一、测试环境

| 项目     | 配置                                                                                |
| ------ | --------------------------------------------------------------------------------- |
| 上位机    | Windows，Electron + Web Bluetooth（仅 localhost 开发环境）                                |
| 主机（左手） | Arduino UNO，蓝牙 SoftwareSerial(2,11) @ 38400，调试走硬件 UART(0/1)                       |
| 从机（右手） | Arduino UNO，与主机同款固件（v1.0）                                                         |
| 协议     | HDLC 帧（0x7E / 转义填充 / CRC16-CCITT-FALSE）+ 命令/ACK + 批量写入会话 + 心跳 PING/PONG           |
| 自动化脚本  | `tests/e2e/glove-link.spec.ts`（断链/恢复）、`tests/e2e/glove-bulk.spec.ts`（批量写入）        |
| 固件     | `trae/trae.ino`（含 handleHeartbeat 每轮重发 PING、lastWriteTarget 目标判定、HELLO 链路状态重置等修复） |

***

## 二、测试用例与结果

### 2.1 蓝牙扫描与连接

- **前置问题**：Windows BLE 扫描广播不稳定，重试 8 轮扫不到 Glove\_L（仅见无关设备）

- **修复**：`requestDevice` 改用 `filters: [{ namePrefix: 'Glove' }]` 聚焦手套广播

- **结果**：✅ 一次扫描命中并成功连接（`Connected to Glove_L`），HELLO → HELLO\_ACK 握手正常

### 2.2 从机链路断链检测（右手机断电）

| 步骤            | 预期          | 实际                                      |
| ------------- | ----------- | --------------------------------------- |
| 连接左主机，右从机保持断电 | \~15s 内上报失联 | ✅ `[11:54:25] Slave link LOST`（连接后 15s） |

- 判定逻辑：主机心跳每轮重发 PING，连续 3 次无 PONG（`heartbeatMiss >= 3`）上报 `EVT_SLAVE_LINK_LOST`

- 关键验证：**断链期间主机心跳不卡死**，PING 持续重发，为后续恢复检测打好基础

### 2.3 从机链路恢复检测（右手机上电）

| 步骤       | 预期               | 实际                                                                            |
| -------- | ---------------- | ----------------------------------------------------------------------------- |
| 给右从机重新上电 | 自动重连 + 校时 + 上报恢复 | ✅ `[11:56:16] Time sync done: RTT 45600us` → `[11:56:20] Slave link restored` |

- 修复生效点：从机恢复后能立即收到新 PING 并回 PONG（旧版 await 死锁已消除），主从链路自动恢复并重新校时

### 2.4 SRAM 容量约束（165 指令 > 32 上限）

- 输入 330 字节（165 指令）写入 SRAM：✅ UI 拦截提示 `SRAM supports up to 32 commands, current 165, excess will be discarded`，**未发起硬件写入**

### 2.5 EEPROM 330 字节批量写入（164 指令级大乐谱传输）

| 指标        | 结果                    |
| --------- | --------------------- |
| 传输分块      | 17 块（块 ACK 独立确认，0x88） |
| 实际字节 / 预期 | **330 / 330**         |
| CRC       | **OK**                |
| 重发次数      | **0**                 |
| 总耗时       | 2.35s                 |

- 链路：`WRITE_BEGIN(0x20)` → 16 × `WRITE_DATA(0x21)` + 尾块 → `WRITE_END(0x22)` → 全 ACK

- 关键验证：**SEQ 错位修复后零丢包零重发**，历史"330 字节写入丢包导致 EEPROM 缺指令"问题已解决

### 2.6 播放 / 停止

- SRAM play → 运行 2s → Stop：✅ 播放启动、停止均正常，无 NAK/失败日志

### 2.7 综合硬件验证（右手写入 + 双手播放）

| 用例                               | 结果 |
| -------------------------------- | -- |
| 蓝牙连接 Glove_L                     | ✅ |
| 左手 EEPROM 330B ×5 轮（稳定性）       | ✅ 5/5（retries=0） |
| **右手 EEPROM 330B（从机转发链）**       | ✅ 330/330，CRC OK，retries=0 |
| 双手 EEPROM 播放（右→左，各 1.5s）      | ✅ 无 NAK |

- 右手路线：`WRITE_BEGIN → WRITE_DATA×17 块 → WRITE_END` 经主机**转发链路**直达从机，数据与 CRC 全部校验通过，验证 DST_SLAVE 转发链可靠

- **控制命令超时优化复验**：控制命令（SRAM play / EEPROM play / Stop / Pause / Resume / Reset）改用 `CTRL_ACK_OPTS = { ackTimeoutMs: 1500, maxRetries: 3 }` 后重跑综合测试（1.2m），日志中 Stop / EEPROM play 均为一次 `→ ACK`，不再出现 ACK 超时误报

### 2.8 真实乐谱 Love Song.mxl（解析 → 指法 → 转换 → 写入 → 播放）

| 步骤           | 结果 |
| ------------ | -- |
| mxl 解析 + OSMD 渲染 | ✅ 93 小节 / 1923 音符 |
| 左手指法自动计算       | ✅ |
| 乐谱 → 指令转换（1-2 小节） | ✅ 33 指令 |
| EEPROM 批量写入      | ✅ 66/66 字节，CRC OK |
| 播放 / 停止         | ✅ |

### 2.9 双手同步序列验证（00 81 40 83 00 87 40 8F 00 9F 40 10 00 08 40 04 00 02 40 01）

| 用例              | 结果 |
| --------------- | -- |
| 同步序列·左手 SRAM 写  | ✅ 20/20，CRC OK |
| 同步序列·右手 SRAM 写  | ✅ 20/20，CRC OK（修复后） |
| **双手同步播放**      | ✅ 命令链路无 NAK，物理节拍同步由佩戴者确认 |

- **修复点**：右手（从机）SRAM 写入曾出现 WRITE_RESULT 丢失（半双工总线碰撞），固件 `finishWriteSession` 增加 20ms 总线静默等待后，`RX seq=0 cmd=0x89` 正常回 App，从"已知问题"转为"通过"

***

## 三、测试结果汇总

| 用例                                             | 结果 |
| ---------------------------------------------- | -- |
| 蓝牙扫描与连接（过滤后一次命中）                             | ✅  |
| 从机断链检测（断电 → `Slave link LOST`）                    | ✅  |
| 从机恢复检测（上电 → 校时 → `Slave link restored`）           | ✅  |
| SRAM 容量约束（165 > 32 拦截）                          | ✅  |
| EEPROM 330 字节批量写入（330/330、CRC OK、0 重发）        | ✅  |
| 左手 EEPROM 330B ×5 轮稳定性                          | ✅ 5/5 |
| 右手 EEPROM 330B 批量写入（从机转发链）                    | ✅  |
| 真实乐谱 Love Song.mxl（解析/指法/转换/写入/播放）            | ✅  |
| 同步序列左右手 SRAM 写入（20/20 ×2，含碰撞修复）             | ✅  |
| 双手同步播放（EEPROM + SRAM 两轮）                      | ✅（物理节拍由佩戴者确认） |
| 播放 / 停止                                          | ✅  |

**Playwright 结果：`glove-link.spec.ts`** **1 passed；`glove-bulk.spec.ts`** **1 passed；`glove-hw-all.spec.ts`** **1 passed（复验轮 1.2m）；`glove-score-sync.spec.ts`** **1 passed（26.9s）**

***

## 四、关键日志摘录

```
[11:54:10] Connected to Glove_L
[11:54:25] Slave link LOST                                 ← 断链上报（断电后 15s）
[11:56:16] Time sync done: RTT 45600us, offset -915860840us ← 从机恢复自动校时
[11:56:20] Slave link restored                             ← 恢复上报

[乐谱写入] 完成 17 块, 实际字节=330, 预期=330, CRC=OK, 重发=0
[bulk EEPROM] Write result: 330/330 bytes, CRC OK, retries 0（Total time: 2.35s）
[SRAM 330B] SRAM supports up to 32 commands, current 165, excess will be discarded
```

***

## 五、结论与遗留事项

### 结论

- 蓝牙协议重构版 v2 的连接、批量传输、从机链路检测三项核心能力**全部验证通过**；右手（从机）写入链路与双手同步也已完成补验

- 固件修复实测生效：heartbeat 每轮发 PING / lastWriteTarget 目标判定 / HELLO 重置链路状态 / finishWriteSession 总线静默，覆盖断链检出、恢复上报、330 字节零重发、从机 WRITE_RESULT 回传

### 遗留事项

1. **双手同步物理确认**：命令链路无 NAK、写入全部成功，但"左右手节拍同步"的最终观感需佩戴手套者实际确认
2. **Web Bluetooth 生产环境限制**：仅在 localhost 开发环境可用，生产发布需用 noble（或等效方案）替换
3. **8MHz 自制板**：固件已预留硬件 UART（8MHz + U2X @38400，误差 0.16%），待自制板出来后切换引脚验证
4. **断链场景下的双向写入**：本次断链/恢复测试以「断电 → 失联 → 上电 → 恢复」为主，写会话中途断链的中止/重试路径未单独压测

