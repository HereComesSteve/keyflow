# 蓝牙指令生成理解（完整链路）

> 目的：彻底搞懂 keyflow 软件 + 固件是如何生成、解析、执行一条蓝牙乐谱指令的，
> 避免以后生成错误指令。验证日期：2026-08-30，BPM=120，左手。

## 一、完整链路总览

```
用户在UI输入纯数据 → 软件解析配对 → 打包成蓝牙帧 → BLE发送
   → 固件接收存储(SRAM/EEPROM) → 播放时算绝对tick → 调度执行LED
```

## 二、第一步：软件端（renderer，TypeScript）

### 1. 用户输入格式

输入框里只填**纯数据**：十六进制字节，空格/换行分隔，**两两配对为** **`[tick, motor]`**。
**不要填同步头** **`0xFB`** **和校验字节**，软件自动加。

例（LED 累积亮灭，每步 1s）：

```
00 81  40 83  00 87  40 8F  00 9F  40 10  00 08  40 04  00 02  40 01
```

### 2. 解析：`parseScoreInput(input)`（[glove-commands.ts](file:///C:/Users/NULL/Documents/key/keyflow/src/renderer/src/lib/bluetooth/glove-commands.ts#L113-L134)）

* 按空格/换行拆 token，两两配对 → `[[tick, motor], ...]`

* 奇数个字节 → 报错 `odd`；非法 hex → 报错 `invalid`

* 配对结果就是 `[tick, motor]`，tick/motor 都是 0\~255 的整数

### 3. 打包：`buildScoreCommand(tick, motor)`（[glove-commands.ts](file:///C:/Users/NULL/Documents/key/keyflow/src/renderer/src/lib/bluetooth/glove-commands.ts#L97-L99)）

```
[0xFB, tick, motor, 0xFB ^ tick ^ motor]
```

* 同步头固定 `0xFB`

* 校验 = `0xFB XOR tick XOR motor`（软件算好，不用手填）

### 4. 发送顺序（[index.tsx](file:///C:/Users/NULL/Documents/key/keyflow/src/renderer/src/components/GloveControlPanel/index.tsx#L472-L543)）

1. 设目标设备：`F9 24 00`（左手）/ `F9 24 01`（右手）
2. 初始化存储：

   * SRAM：`F9 10 00 00`（清空）

   * EEPROM：`F9 12 [分区] 00`（清空）+ `F9 11 [分区] 00`（进入写模式）
3. 逐条发 `FB tick motor XOR`，每条间隔 **5ms**
4. 发结束标志 `FB FF 00 04`（校验固定 0x04）

### 5. 播放指令（写完后再发）

* SRAM 播放：`F9 04 00 00`

* EEPROM 播放：`F9 05 [分区] 00`

* **不要逐条手动发**：手动发是立即执行，tick 不生效；必须先写入再播放

## 三、从乐谱导入指令（ScoreConverter 自动生成）

> 在手套控制面板点「从当前乐谱导入」，把已加载乐谱 + 指法一键转成蓝牙指令填入输入框。
> 文件：[ScoreConverter.ts](file:///C:/Users/NULL/Documents/key/keyflow/src/renderer/src/lib/glove/ScoreConverter.ts)

### 0. 前置条件（[index.tsx](file:///C:/Users/NULL/Documents/key/keyflow/src/renderer/src/components/GloveControlPanel/index.tsx#L682-L719)）

* 已加载乐谱（`score.measures` 非空）

* 有指法标注（`annotations` 非空，且目标手音符带 `fingerNumber`）

* 目标手存在带指法的音符，否则报错不导入

### 1. 解析小节范围 `parseRange`

* `"1-3"` → \[1,2,3]；`"1-3, 5-7"` → \[1,2,3,5,6,7]；`"1-3,1-3"` → 重复两次（实现反复效果）；空 → 全部小节

* 无效输入（start>end、非数字、<1）抛错

### 2. 按小节序列生成 On/Off 指令

关键参数：

* `ppq = score.ticksPerQuarter`（乐谱 PPQ = 480）

* `beats / beatType` = 拍号（如 4/4）

* **蓝牙 tick 空间：每拍固定 32 tick，`ticksPerBar = 32 × beats`**（4/4 → 128）

每个小节（segIdx 从 0 起）：

* `tickOffset = segIdx × ticksPerBar`（**重复小节也占独立偏移**，所以 "1,1" 会生成两遍）

* 对每个音符（非休止、目标手、有指法）：

  * `onRelScoreTick = note.startTick - measure.startTick`（小节内相对 tick）

  * `offRelScoreTick = onRelScoreTick + note.durationTicks`

  * 换算蓝牙 tick：`toBluetoothTick(scoreTick) = round(scoreTick × 32 / (ppq × 4/beatType))`

  * `onAbsTick = 换算(onRelScoreTick) + tickOffset`

  * On 指令：`motorCtrl = 0x80 | (1 << (finger-1))`（bit7=1）

  * Off 指令：`motorCtrl = 0x00 | (1 << (finger-1))`（bit7=0）

* **同 (absTick, isOn) 的多手指 OR 合并**（和弦一键按下）

### 3. 排序

* 按 `absTick` 升序；**同 tick Off（bit7=0）在前、On（bit7=1）在后**（先停旧音再起新音）

### 4. 空小节处理 `handleEmptyMeasures`（必须在 applyPatch 之前）

固件只靠"相邻 tick 递减"识别跨小节。**某小节完全没有指令（整小节休止）时失去锚点 → 后续所有指令时间轴错位**。

* 完全空的小节：在 `tick=0` 和 `tick=ticksPerBar-1` 各插一条 `motorCtrl=0x00` 空指令

* 第一小节末尾无指令（relTick < ticksPerBar-1）：在 `tick=ticksPerBar-1` 补一条空指令（弱起处理）

### 5. 递归补丁 `applyPatch`（解决固件跨小节检测问题）

固件只认 `if (curTick < lastTick) 跨小节`。**若音符跨小节且相对 tick 递增**（如上一小节 rel=96 → 下一小节 rel=32），固件检测不到跨小节 → 时间轴错位。

* 在跨小节边界插入**一对"保持当前状态"指令**：

  * `endOfBar = (bar+1)×ticksPerBar - 1`（小节末尾）

  * `startOfNextBar = (bar+1)×ticksPerBar`（下小节开头）

  * motorCtrl = `0x80 | 当前累积掩码`（把"当前亮着的指"重发一遍，维持状态同时制造跨小节递减）

* 每次遍历维护 `accumulatedMask`（On 加入掩码 / Off 移除掩码）

* **递归迭代**直到不再需要补丁（上限 100 次防死循环）

### 6. 弱起小节处理

* 弱起 = 第一小节不完整（只有半小节）

* 若第一小节最后一条指令的 relTick < ticksPerBar-1（小节末尾没指令），在 `tick=ticksPerBar-1` 补一条空指令

* **目的**：让第一小节末尾 relTick 到 127，第二小节开头 relTick 回到 0，形成 "127 → 0" 递减 → 固件才能识别跨小节

### 7. 输出格式

* 每条输出 `relTick motorCtrl`，其中 **relTick = absTick % ticksPerBar**（每小节内 0\~127 循环递增）

* 末尾追加 `FF 00` 结束标志

* 检查是否超 SRAM 32 条上限（UI 提示）

### 8. 一个小节内有和弦时同 tick 排序示例（测试用例）

```
00 81   C4 On  (abs=0,   rel=0)
20 01   C4 Off (abs=32,  rel=32)  ← 同 tick Off 在前
20 82   E4 On  (abs=32,  rel=32)  ← 同 tick On 在后
40 02   E4 Off (abs=64,  rel=64)
40 84   G4 On  (abs=64,  rel=64)
60 04   G4 Off (abs=96,  rel=96)
60 90   C5 On  (abs=96,  rel=96)
7F 00   弱起小节空指令 (abs=127, rel=127)  ← 第一小节末尾补锚点
00 10   C5 Off (abs=128, rel=0)   ← 第二小节 relTick 重新从 0 开始
FF 00   结束标志
```

## 四、第二步：固件端（Arduino，trae.ino）

### 1. 接收解析：`parseCommand()`（[trae.ino](file:///C:/Users/NULL/Documents/key/keyflow/trae/trae.ino)）

* 主循环按 4 字节帧同步：`0xFB` 开头 → 乐谱数据

* `0xFB` 分支：`b1` = tick 存入 `cmdTicks[]`，`b2` = motor 存入 `cmdMotors[]`

* 收到 `FF 00` → 结束标志，停止存储

* 校验：`enableXorCheck` 默认 false（调试模式不校验）；开启时 `0xFB ^ b1 ^ b2 == b3` 才接收

### 2. 存储

* **SRAM**：`cmdTicks[]`/`cmdMotors[]`，上限 `MAX_CMDS = 32` 条，超出丢弃

* **EEPROM**：每条 2 字节 `[tick, motor]`，`FF 00` 为分区结束标志

### 3. 播放调度：`startPlayback()` 算绝对 tick（关键！）

固件不直接用 tick 当时间，而是\*\*先累加成"绝对 tick"\*\*再调度：

```
absT = cmdTicks[0]
for i = 1..N:
    if cmdTicks[i] < cmdTicks[i-1]:   # tick 变小 = 跨小节
        absT += totalTicksPerBar - cmdTicks[i-1] + cmdTicks[i]
    else:
        absT += cmdTicks[i] - cmdTicks[i-1]
```

* `totalTicksPerBar = 拍数 × ticksPerBeat = 4 × 32 = 128`（4/4 拍）

* 然后每帧比较 `elapsedTicks >= sramAbsTicks[i]` 决定是否执行该条

**由此推出的 tick 设计铁律：**

1. **tick 值必须 < 128**（`totalTicksPerBar`），否则回绕公式会算错
2. **跨小节必须用"tick 变小"表达**：相邻两条间隔 64（0x40）后，下一条回到 0
3. **绝不能用连续相同或一直递增的 tick** —— 那样两条会被算到同一绝对 tick，**同一时刻瞬间全部执行**
4. 1 秒 = 64 tick（BPM=120 时 `tickDurationUs = 60000000/120/32 = 15625µs`）

   * tick 间隔换算：`每秒tick数 = BPM × 32 / 60 = BPM × 0.5333`

### 4. motor 编码（`executeCommand()`）

```
bit7 = 0x80：亮/灭开关（1=亮, 0=灭）
bit0~bit4：LED1~LED5 掩码（01/02/04/08/10）
```

* 亮：`0x80 | 掩码`；灭：`0x00 | 掩码`

* 只操作掩码指定的灯，**其余灯保持原状态**（所以"灭LED5"只要 `0x10`，不用管其他灯）

## 五、本次实战数据（LED 累积亮灭，BPM=120）

效果：`1 → 12 → 123 → 1234 → 12345 → 1234 → 123 → 12 → 1 → 全灭`，每步 1s

```
00 81  40 83  00 87  40 8F  00 9F  40 10  00 08  40 04  00 02  40 01
```

| 步  | tick | motor | 效果     | 绝对tick |
| -- | ---- | ----- | ------ | ------ |
| 1  | 00   | 81    | LED1 亮 | 0      |
| 2  | 40   | 83    | +LED2  | 64     |
| 3  | 00   | 87    | +LED3  | 128    |
| 4  | 40   | 8F    | +LED4  | 192    |
| 5  | 00   | 9F    | 全亮     | 256    |
| 6  | 40   | 10    | LED5灭  | 320    |
| 7  | 00   | 08    | LED4灭  | 384    |
| 8  | 40   | 04    | LED3灭  | 448    |
| 9  | 00   | 02    | LED2灭  | 512    |
| 10 | 40   | 01    | LED1灭  | 576    |

（绝对 tick 模拟验证：相邻间隔全部 = 64 tick = 1.00s，无同时执行）

## 六、易踩的坑（务必记住）

1. **tick 没按 00/40 交替**（填了 40 40 40 或一直递增）→ 多条指令同一绝对 tick → **瞬间同时亮/灭**
2. **BLE 丢包** → 某条指令没写进 EEPROM → 播放时**少一个动作**，而且 EEPROM 读到结束标志后会**循环重启全灭**，看起来像"最后两步同时熄灭"

   * 本次教训：`00 02`(灭LED2) 后缺了 `40 01`(灭LED1)，表现就是 "12 → 0"

   * 排查法：看固件串口 `EEPROM read: addr=...` 的地址是否连续、`Bytes written` 是否 = 条数×2+2
3. **间隔不对** → BPM 不是 120
4. **灯该灭不灭** → motor 掩码/亮灭位填错

## 七、生成新指令的通用方法（防错流程）

1. 确定效果序列（每步灯的状态）
2. 确定每步间隔秒数 → tick 间隔 = 秒数 × BPM × 0.5333（BPM=120 时 1s = 64）
3. tick 从 0 开始，严格 00/40 交替（或"间隔值/0"交替），**每步 tick 都 < 128**
4. motor：亮用 `0x80|掩码`，灭用 `0x00|掩码`（只写要变的那只灯）
5. 用上面的"绝对 tick 累加公式"心算一遍相邻间隔，确认没有相同绝对 tick
6. 写入 EEPROM/SRAM 后先看 `Bytes written` 确认条数完整，再播放

